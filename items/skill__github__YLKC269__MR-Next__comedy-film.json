{
  "id": "skill:github/YLKC269/MR-Next/comedy-film",
  "type": "skill",
  "name": "comedy-film",
  "description": "Comedy film with humor and situational jokes.",
  "author": "YLKC269",
  "source": {
    "registry": "github-search",
    "url": "https://github.com/YLKC269/MR-Next/blob/HEAD/skills/comedy-film/SKILL.md",
    "repo": "YLKC269/MR-Next",
    "path": "skills/comedy-film/SKILL.md"
  },
  "license": "Apache-2.0",
  "install": {
    "kind": "skill-dir"
  },
  "quality": {
    "stars": 3,
    "pushed_at": "2026-09-12T08:09:11Z",
    "score": 57,
    "star_history": [
      {
        "d": "2026-09-12",
        "s": 3
      }
    ]
  },
  "security": {
    "score": 100,
    "scanned": true,
    "checks": [
      {
        "name": "描述层注入",
        "pass": true
      },
      {
        "name": "敏感路径",
        "pass": true
      },
      {
        "name": "外部渗出",
        "pass": true
      },
      {
        "name": "危险执行",
        "pass": true
      },
      {
        "name": "配置风险",
        "pass": true
      }
    ]
  },
  "tags": {
    "category": null,
    "featured": false
  },
  "readme": "# MR分镜助手导演台 · Next — 安装流程与使用教程\n\n> 版本：**v1.11.16** ｜ 一句话定位：**在一个节点里跑完整条 AI 短剧流水线** —— 剧本 → 拆分分镜 → 生成设定图 → 匹配素材 → 逐镜出片 → 剪辑成片。\n>\n> 官方 MiniMax H3 出片引擎已内嵌在包内，**不需要另外安装任何节点**（VOSR2 / SeedVR2 是可选增强，装了才启用）。\n\n<details open>\n<summary><b>本次更新 · v1.11.15（点开看变化）</b></summary>\n\n**移除 TE 加速（TE-Speed / `TESpeedMiniMaxH3`）**\n\n「⚡ 加速」页原来的 **「TE-Speed 模式」** 和 **「TE-Speed 设备」** 两个控件已整体删除，\n连带清掉了它带来的**隐藏依赖**与**画质风险**：\n\n- 节点参数不再输出 `speed_node` / `speed_device` → 构图里不会再插入 `TESpeedMiniMaxH3` 节点。\n- 「一键最佳画质」与「加速状态」总览里的 TE-Speed 项同步删除（少一个要排查的开关）。\n- 外接第三方加速节点时的「让路」白名单里也去掉了 `speed_node` / `speed_mode`。\n- **不再需要额外安装 `TE-speed-minimaxH3` 这个可选增强包**（未装也不影响任何功能）。\n\n> 说明：**移除的只是「采样步数加速」**。二采超分链路里的 `TESpeedVideoCombine`\n> （TE-Speed-FlashVSR 超分）**保持不动**，出片后二采不受影响。\n\n> 如果你之前开过 TE-Speed：它只是从设置里消失了，**不会**影响出片路径 ——\n> 现在模型链是 `UNET → [LoRA] → [蒸馏 LoRA]`，步数照旧由「步数 / 画质档位」控制。\n> 旧的本地设置里残留的 `speed.node` 键会被忽略，不需要手动清理。\n\n**对齐官方导演台：修掉 4 处「面板调了不生效 / 与官方不一致」**\n\n按官方 vendored 引擎（`vendor/ComfyUI_MiniMaxH3_Director`）+ ComfyUI 内置\n`comfy_extras/nodes_minimax_h3.py` 逐项核对后，修了这些真偏差：\n\n| 问题 | 之前 | 现在 |\n|---|---|---|\n| **面板 FPS 白调** | 只写 `widget.frame_rate`，不写 `timeline_data.frameRate`；官方 `plan.py` 优先读后者 → **恒被模板的 24 覆盖** | 两个都写，`timeline_data.frameRate` 跟随面板（实测设 30 → 写进 30.0） |\n| **段间重叠帧默认非法** | 默认 **9**，而官方合法集只有 **5/22/39/56**（`snap_context_frames`），9 会被归成 5 → 设了等于没设 | 默认改 **22**（= 官方默认），下拉只留 5/22/39/56 |\n| **「段间清显存」关不掉** | 只在 `true` 时下发；取消勾选 → 字段被删 → 官方读到模板默认 `true` | 布尔值**始终下发 true/false**，且默认对齐官方 `True` |\n| **`ref_image_size` 完全缺失** | 官方有 `match` / `max` 组合框（`max` = 参考图短边 2048，身份保真最好），本项目恒 `match` | 新增「**参考图缩放**」下拉（match/max），写进 `output.refImageSize` |\n| **流水线与单镜不一致** | 一键流水线没下发 `attention_accel` → 流水线出片时内置注意力加速恒 off | 流水线同步下发 |\n\n**台词改用官方 `<d>…</d>` 格式**\n\n`<d>` / `</d>` 在模型侧是**真实特殊 token**（`comfy/text_encoders/minimax.py` 里 id 151669/151670），\n官方 Prompt Guide 明确规定「`<d>` 内只放语言标签 + 原话，身份/编号/语气/音色参考写在 `<d>` 外」。\n之前台词没包 `<d>`，模型只当普通文本 → 口型与音色对齐都变差。现在：\n\n```\n阿明 (S1) says: <d>[Chinese] 你好，好久不见。</d>\n```\n\n音色参考 `(voice reference <Audio 1>)` 仍然保留在同一行、但在 `<d>` **之外**（既满足官方语义，\n也保住「音色参考必须绑在它配的那句台词上」这条实测结论）。\n\n</details>\n\n<details>\n<summary><b>上一版 · v1.11.14（点开看变化）</b></summary>\n\n**① 视频「好糊、不如以前清晰」—— 把吃画质的开关摆到明面上 + 一键回退**\n\n结论先说：**加速就是「速度换画质」**。视频变糊几乎都是下面这些开关被打开了，\n而且它们**全部存在本地（localStorage）** —— 重开节点、重启服务都不会自己恢复：\n\n| 开关 | 为什么画面会变软 |\n|---|---|\n| **步数 `steps`** | 节点原生默认 **25**；「画质档位」按钮会把它改成 **标准 = 12**、**草稿 = 6** ← 最容易踩的一脚 |\n| **蒸馏 LoRA** | 4/8 步蒸馏权重，本来就是给极低步数训练的，细节明显偏软 |\n| **外接 SageAttention** | int8 量化 attention |\n| **内置注意力加速** | 同上（`sage` / `block_sparse` 都是 int8 量化） |\n| **出片后二采没开** | 二采（放大精修）能明显提清晰度，关了就少一道 |\n\n新增三件事：\n\n1. **「⚡ 加速」页顶部「加速状态」总览**：直接写出当前开着哪几项、步数多少、二采开没开、\n   **画幅多少 MP**；步数 < 20 会明确提醒「节点原生默认 25，越低越糊」。\n2. **「🧼 一键最佳画质」按钮**（「⚡ 加速」页 和「⚙ 采样设置 → 画质档位」两处都有）：\n   一键设成 **25 步 + 出片后自动二采 + 关掉全部加速**，并在提示里逐条列出改了什么。\n   觉得糊就点一下，既回到了最清晰的配置，也能立刻对比出是哪个开关吃掉了画质。\n3. **画质档位名直接带步数**：`草稿 6 步（快·最糊）` / `标准 12 步` / `成品 20 步+二采`，\n   不再出现「选了『标准』却不知道它把 25 步降成了 12 步」。\n\n> 建议对比法：先用当前设置出一镜 → 点「🧼 一键最佳画质」→ **同 seed** 再出一镜 → 对比。\n> 若两者一样糊，问题不在加速，而在素材本身或画幅（百万像素 / 宽高）太低。\n\n**② 修复 `<Audio N>` / `<Video N>`「找不到对应音频」误报**\n\n本镜「图5 视0 音2」，正文里 `<Audio 3>` 却被报「找不到对应音频（本镜音频槽只有 2 条）\n→ 这几条音色参考不会生效」——**这是误报**。\n\n根因：警告拿**本镜格子的第 N 格**当判据，而后端 `h3shot._compact()` 认的是**编号 N 本身**：\n\n```\n只看 1 ≤ N ≤ 槽位上限(3) 且能解析出素材的编号条目；格子里的算「没标编号的额外参考」\n```\n\n两套编号不是一回事 → 素材库有第 3 个音频、而格子里恰好没占第 3 格时就会误报\n（编号 → 素材的对应关系走前端发过去的 `byNum`，与「格子填了几条」无关）。\n\n现在判据与后端完全对齐，只在**真会失效**时告警，并且说清原因与改法：\n\n- `<Audio N>` / `<Video N>` 编号 **> 3** → 「编号超出上限（官方只有 3 个槽位），会被丢弃」\n- 编号 1–3 但**解析不出素材**（没有手动绑定、素材库也没有第 N 个）→「解析不到素材」+ 具体建议\n\n</details>\n\n<details>\n<summary><b>上一版 · v1.11.13（点开看变化）</b></summary>\n\n**① 新增「内置注意力加速」（可选，默认关闭）**\n\n导演台 **⚙ 采样设置** 页新增 **「内置注意力加速」** 下拉，直接作用在本节点自己的采样路径上，\n**不需要在画布上外接任何加速节点**：\n\n| 选项 | 说明 | 本机 |\n|---|---|---|\n| `关闭（官方 attention）` | 默认。最稳，出片行为与以前完全一致 | ✅ |\n| `SageAttention（int8）` | H3 官方「加速版」工作流同款路径（`sageattn` int8 TensorCore） | ✅ 已装可用 |\n| `官方 Block-Sparse-Attention` | mit-han-lab 的 `block_sparse_attn_func`，128×128 块稀疏，支持 **sm_80–sm_100** | ⚠ 需本地编译 |\n\n**② 三条安全铁律（沿用双时钟的教训）**\n\n1. **默认关闭**：`attention_accel` 默认 `off`，不开就完全走官方 attention，零行为差异。\n2. **失败必回退**：所选后端不可用 / 中途抛错 → **自动降级**（`block_sparse → sage → off`）\n   并在日志与运行报告里留一条告警，**绝不中断采样、绝不影响出片**。\n3. **真探测，不猜**：后端可用性来自真实的 `import` + 算力检查 + **一次小张量冒烟**，\n   而不是\"模块名在不在\"。可用性结果带缓存，同进程只真跑一次。\n\n**③ 落地方式**\n\n- 后端：`vendor/ComfyUI_MiniMaxH3_Director/director/attention_accel.py`\n  —— 通过 ComfyUI 原生的 `transformer_options[\"optimized_attention_override\"]` 通道打补丁，\n  比逐 block 改 `forward` 更轻、更不容易和别的补丁打架。\n- 探测接口：`GET /mrnext/h3/attention_accel`（前端下拉旁的 ✓/⚠ 提示）。\n- 运行报告里会打印实际生效的加速模式 + 可用性摘要，**开没开、有没有降级都看得见**。\n- 换了加速模式会变更分镜缓存 key，不会误命中旧缓存。\n\n**④ 关于 Block-Sparse-Attention（本机装不上的原因与编译指引）**\n\n这个 kernel **没有预编译轮子**（PyPI 无包、GitHub release 只有源码），必须本地编译。\n本机当前 **编译不了**，原因是 CUDA 工具链主版本不匹配：\n\n```\nRuntimeError: The detected CUDA version (12.6) mismatches the version\nthat was used to compile PyTorch (13.2).\n```\n\n- 本机 `nvcc --version` = **CUDA 12.6**，而 torch = **2.12.1+cu132**（CUDA 13.2）。\n  torch 的 `cpp_extension._check_cuda_version` 里主版本不同会**直接抛错**，没有环境变量能绕过。\n- `C:/Program Files/NVIDIA GPU Computing Toolkit/CUDA/` 下只有 `v12.6`（有 nvcc）\n  和 `v13.1`（**只有 extras，没有 nvcc**）。\n\n想启用 Block-Sparse 的话，装一个 **CUDA 13.x 的完整 toolkit**（含 nvcc），然后：\n\n```bash\ngit clone https://github.com/mit-han-lab/Block-Sparse-Attention\ncd Block-Sparse-Attention\n# 让 torch 找到 13.x 的 toolkit（把它放在 CUDA_PATH 或 --cuda-home 指过去）\npython setup.py install            # 或 pip install --no-build-isolation .\n```\n\n装好之后**不用改任何设置**：`/mrnext/h3/attention_accel` 的冒烟探测会自动变成 ✓，\n下拉里的 Block-Sparse 立即可选。在那之前，选它也会**自动降级到 SageAttention** 并告知原因。\n\n> 本机显卡是 **RTX 4090 Laptop（sm_89）**，在 kernel 支持的 sm_80–sm_100 范围内，\n> 所以只要编译问题解决就能用。\n\n**⑤ 修复「点面板按钮节点塌掉 / 面板崩掉」**\n\n现场：节点被压到约 **80px** 宽，面板内容横向溢出被裁切，看起来就是\"崩了\"。\n\n根因有两层：\n\n1. **节点尺寸锁只单向**。`main.js` 里原来的 `fixSize()` 只在 `onExecuted` 跑，\n   而且**只压回\"变大\"的情况**（`s[0] > W+4`）。一旦 ComfyUI 在 DOM widget 重排后\n   按 `computeSize()` 把节点**压小**，就永远修不回来 —— 面板从此一直是那条细缝。\n   现在改成 **双向钳制 + 挂到 `onResize` / `onAdded`**：宽度高度不得低于设计值\n   （1100×860），放大不干预。压小后**立刻自愈**。\n   顺带把 `node.size` 的 `defineProperty` 补上 `enumerable: true`\n   —— 不可枚举会让部分 clone/序列化路径丢掉 `size`，载入回来节点就塌了。\n\n2. **在控件的 `change` 事件里重建整个参数面板**。加速下拉原来在 `onchange` 里调了\n   `renderParam()`，而 `renderParam()` 会 `clear(paramRow)` —— 把**正在派发 change 事件的\n   那个 `<select>` 从 DOM 上摘掉**并重建整页。现在改成直接改兄弟控件的值，不重建 DOM。\n\n**⑥ 布局修复：auto-fill 网格里不再写死 `gridColumn: \"span N\"`**\n\n`.tl-paramrow` 是 `repeat(auto-fill, minmax(148px, 1fr))`，**列数随容器宽度变**。\n写死 `span 3` / `span 6` 会在窄容器下强行撑出 3/6 列 → 网格比容器宽 → 横向溢出被裁切。\n已全部改为 `1 / -1`（跨满实际列数，永不撑列）。\n新增的加速状态备注也改成和下拉**同一个 cell**，不再单独占一个网格单元。\n实测：节点 420px 宽时参数网格横向溢出 **0px**（修复前会撑出 6 列）。\n\n**⑦ 修复「视频好糊，不如以前清晰」——把吃画质的开关摆到明面上 + 一键回退**\n\n先说结论：**加速就是「速度换画质」**。出片变糊几乎都是下面这些开关被打开了\n（它们全部持久化到 localStorage，重开节点也不会自己恢复）：\n\n| 开关 | 为什么会让画面变软 |\n|---|---|\n| **步数 `steps`** | 节点原生默认 **25**。而「画质档位」按钮会把步数改成「标准 **12**」「草稿 **6**」—— 这是最容易踩的一脚 |\n| **蒸馏 LoRA `speed_lora`** | 4/8 步蒸馏权重，本来就是为极低步数训练的，细节明显偏软 |\n| **外接 SageAttention `sage_attention`** | int8 量化 attention |\n| **内置注意力加速 `attention_accel`** | 同上（sage / block_sparse 都是 int8 量化） |\n| **出片后二采未开** | 二采（放大/精修）能明显提清晰度，关掉就少了一道 |\n\n现在做了三件事：\n\n1. **「⚡ 加速」页顶部新增「加速状态」总览**，直接写出当前开着哪几项、步数多少、\n   二采开没开；步数 < 20 会明确提醒「节点原生默认 25，越低越糊」。\n2. **新增「🧼 一键最佳画质」按钮**（「⚡ 加速」页和「⚙ 采样设置 → 画质档位」两处都有）：\n   一键设成 **25 步 + 出片后自动二采 + 关掉全部加速**，并在 toast 里逐条列出改了什么，\n   所以「糊」的时候点一下就能回到最清晰的配置，也能立刻对比出是哪个开关吃掉了画质。\n3. **画质档位的档位名直接带上步数**：`草稿 6 步（快·最糊）` / `标准 12 步` / `成品 20 步+二采`，\n   避免「选了『标准』却不知道它把 25 步降成了 12 步」。\n\n> 建议的对比方法：先用当前设置出一镜 → 点「🧼 一键最佳画质」再出同一镜（同 seed）→ 对比。\n> 如果两者一样糊，那问题不在加速，而在**素材本身**或**画幅（百万像素/宽高）太低**。\n\n</details>\n\n<details>\n<summary><b>上一版 · v1.11.12（点开看变化）</b></summary>\n\n**① 修复「完全出不了片」：T8 双时钟不再阻断采样**\n\n线上报错（`miniMaxH3Director` 节点，`r2v` / `608×1024` / 192 帧）：\n\n```\nValueError: 无法从 AV latent 解出双时钟切分：latent 形状=()、latent_shapes=None\n  core_sampling.py:101 _dual_clock_latent_shape\n```\n\n原因是双时钟需要把 packed latent 切回「视频段 / 音频段」，而在 `FLOW_AV` 这条链路上\nlatent 既不是 nested、模型也没有给出 `latent_shapes` —— 三种来源全部落空，\n旧代码在这里**直接抛错**，于是整段视频一步都跑不了。\n\n现在改成 **解不出就回退官方单时钟**（并打一条告警），**绝不抛错阻断出片**。\n\n**② 双时钟默认关闭，采样回到官方路径**\n\n- 节点输入 **`dual_clock` 默认改为 `False`**（原来是 `True`），`steps_audio` 默认 `0`。\n- `dual_clock=False` 时采样完全走官方链路：\n  `MiniMaxH3SigmaShift(12/3)` → `BasicScheduler` → `KSamplerSelect` → `SamplerCustomAdvanced`。\n- 双时钟保留为**可选实验分支**：想试就手动把开关打开；开关打开但切分解不出时，\n  仍然自动回退官方单时钟 —— 也就是说**任何情况下都能出片**。\n\n> 旧的已保存工作流里如果残留 `dual_clock=true`，行为也不再是\"开不了就跑不了\"，\n> 最坏情况是打一条告警然后照常按官方单时钟出片。\n\n</details>\n\n<details>\n<summary><b>上一版 · v1.11.11（点开看变化）</b></summary>\n\n**① 时间线面板「视频分辨率」不再重复 —— 只保留顶部一份**\n\n同一个时间线面板里以前有**两套**完全一样的分辨率设置：一套在「📐 采样设置」页里，\n一套在分镜上方的工具栏行里。两套都读写同一个共享状态（`store.vidSize / vidW / vidH / vidMP`），\n所以改哪套都生效 —— 但摆在一起就是重复，容易让人以为\"改了这个那个没变\"。\n\n现在**只保留工具栏那一套**（顶部一行：`分辨率 [档位▾] 宽 高 MP 朝向`），\n「采样设置」页里那套连同它的「H3 百万像素」输入框一起移除。\n两边共用同一个状态源，工具栏改完，「一键流水线」面板照常联动。\n\n**② 百万像素（MP）支持自选「横屏 / 竖屏」**\n\nMP 的算法本身是**按当前宽高比**算出尺寸（`W = round(aw·√(MP·1024²/(aw·ah))/32)·32`），\n所以以前想要横屏就必须先去档位下拉里挑一个横屏档、再去填 MP —— 绕。\n\n现在 MP 输入框右边多了一个**朝向**下拉：\n\n| 选项 | 行为 |\n|---|---|\n| **按比例**（默认，等同旧行为） | 沿用当前宽高比，只按 MP 缩放 |\n| **竖屏 ▯** | 结果强制 `高 > 宽` |\n| **横屏 ▭** | 结果强制 `宽 > 高` |\n\n- **MP 已填**时切换朝向：立刻按新朝向重算宽高，**MP（总像素）保持不变** ——\n  例如 `0.92MP` 在竖屏是 `736×1312`，切横屏变 `1312×736`，还是 0.92MP。\n- **MP 留空**时切换朝向：自动挑一个对应朝向的档位（竖屏↔9:16，横屏↔16:9），\n  并**优先选像素规模最接近的那个镜像档**（`720×1280 ⇄ 1280×720` 同为 0.92MP）。\n- 朝向也会写进共享状态（`store.vidOrient`），「一键流水线」面板读得到，两个面板不会打架。\n\n</details>\n\n<details>\n<summary><b>历史更新 · v1.11.10（点开看变化）</b></summary>\n\n**① 🔴 修掉「开了双时钟就完全出不了片」——两个真 bug**\n\nv1.11.9 引入的 T8 双时钟采样（节点默认 `dual_clock=True`）有**两处实现错误**，前者直接让出片失败、后者把音频推爆：\n\n**bug 1：AV latent 切分算错 → 采样第一步就抛异常，一张图都出不来**\n\n官方 H3 的 latent 是「video + audio」两段流。`comfy.utils.pack_latents` 会把每段各自 reshape\n成 `[B, 1, prod(shape[1:])]` 再沿最后一维 **cat** 起来 —— 所以 packed 总长是各流元素数 **相加**。\n\n旧代码却写成了 **相乘**：\n\n```python\npv = vv                      # video 元素数\nfor d in streams[1].shape[1:]:\n    pv *= int(d)             # ❌ 又乘上 audio 的每一维\n```\n\n1344×768 / 124 帧时算出 `packed_values = 11,903,238,144`，而真实值是 `3,003,776`。\n于是采样器入口的断言 `x.shape[-1] != packed_values` 立刻命中：\n\n```\nValueError: H3 packed latent 在采样器建立后发生改变：期望 11903238144 个值，实际 3003776\n```\n\n因为 `dual_clock` 默认开启，**等于默认状态下无法出片**。\n\n修法：切分改为「各流元素数之和」，并新增权威来源 —— 采样前 ComfyUI 会把\n`latent_shapes`（每流原始形状）挂到模型上，现在优先读它；采样器入口还用 `x` 的真实\n长度做一次交叉校验（对不上就按 `latent_shapes` 重算，而不是静默用错值）。\n\n**bug 2：音频步数少于视频时，音频被反复超推 → 爆音/白噪声**\n\n节点默认 `steps=25 / steps_audio=8`。旧映射在音频索引用「到顶就夹住」的写法：\n\n```python\na_start = min(step, n_audio - 1)     # ❌ 第 8 步之后永远夹在最后一段\na_end = max(a_start + 1, ...)        # ❌ 强制 +1，凭空多推一步\n```\n\n结果第 8~24 步**每步都重复套用最后一段 Δσ_a**，音频累计推进被放大成\n**-6.1**（它自己的时钟全程只有 **-1.0**）→ 音频被推到自己时钟之外很远处。\n这正是双时钟本该避免的「音频被拉爆」，等于把要解决的问题又种了回去。\n\n修法：改用**归一化位置线性插值** —— 视频第 `step` 步对应音频序列上的分数位置\n`step/n_steps × (len-1)`，在相邻两点间插值取 σ_a。这样音频累计推进量**严格等于**\n它自己的 σ_a 全程（1→0），既不截断也不超调，且每步的 Δσ_a 依旧比 Δσ_v 小\n（「音频一步一步慢慢走」的完整性机制保住）。\n\n回归：`.mmx_tests/t_dual_clock.py` 从 30 项扩到 **36 项全绿**，新增第 11/12 组专门盯\n「`steps_audio < steps`」（也就是节点默认档）与「packed 总长必须相加」。\n\n</details>\n\n<details>\n<summary><b>历史更新 · v1.11.9（点开看变化）</b></summary>\n\n**① 时间线布局：实时预览框加宽，提示词文本框不再霸占整行**\n\n用户反馈「时间线区域的文本框太宽了，挤压了实时预览的空间」。旧实现里 `.mm-prompt` 是\n`flex:1 1 auto` 且**没有上限** → 它吃掉全部剩余宽度，而预览框被钉死在 `15.5cqw`（971px 容器下只有约 150px），\n两者严重失衡。\n\n修法：\n\n- 新增统一变量 `--mm-pv: clamp(168px, 22cqw, 280px)`，预览框宽高都由它给出（**仍是正方形**，不跳版）；\n- 给 `.mm-prompt` 加 `max-width: 680px`，富余空间主动让给预览框；\n- 预览框 `margin-left:auto`，始终贴右。\n\n实测（容器 972px、r2v 三栏）：素材 242px ｜ 提示词 357px ｜ **预览 214px**（旧约 150px，**+43%**），三栏合计 813px 正好铺满、无溢出。\n\n**② 修掉「点标记替换配音后，显示的音色不是刚点的那个」**\n\n分镜/时间线面板点 `<Audio N>` chip、在宫格选一个新音色后，toast 说「已替换为 `<Audio 3>`（春桃配音.wav）」，\n但 chip 上显示的是 **`沈砚之配音.wav`** —— 送错音色给模型。\n\n根因：这条路径走的是 `replaceFirstTag`，往正文写的是**位置号** `<Audio N>`；而素材库一旦拖拽换序，\n`reindexByKind` 会静默重编号，`<Audio N>` 就指向了别的文件。后端也按位置号解析\n（`<Audio N>` → `ref_audios.ref_audio_{N-1}`），所以前端写错号 = **真的把错音色送进模型**。\n\n修法：非权威点击在改完文本后**同时写一条按 rel 的身份绑定** `assetRegistry.setTagBinding(\"<Audio N>\", rel)`。\n`tagTokenHTML()` 是绑定优先的，于是 chip 按素材身份解析，彻底免疫重排。\n\n**③ 新增：出片音频完整性审计（T8 双时钟的「双保险」之一）**\n\n移植 T8 的 `audio_integrity_advanced` 为 `director/audio_integrity.py`，**只报告、不改动音频**\n（`report_only=True`）。在 `executor_core.py` 组好 `export_audios` 后统一审计，结果写进出片 report。\n\n采用**两级 severity**，避免误报刷屏：\n\n- `hard`：音画边界不匹配（容差 21ms ≈ 半帧 @24fps）、NaN/Inf、削波、爆音、直流跳变 → 驱动 ABSTAIN + 写告警行；\n- `advisory`：尾接头（结尾与开头高度相似）→ **只进 JSON report**。因为周期性配乐/环境底噪/纯音都会命中\n  （实测一段 2.0s 纯正弦必然触发，correlation 0.99999），拿它当告警只会制造噪音。\n\n审计函数绝不抛异常：失败返回 `(True, \"SKIPPED\", {...})`，不会拖垮出片。\n\n</details>\n\n<details>\n<summary><b>上一版 · v1.11.8（点开看变化）</b></summary>\n\n**① 修掉「点击标记替换配音标记不会变」**\n\n在分镜正文里点 `<Audio 2>` chip → 宫格弹层里选一个新音频，chip 上显示的素材名却纹丝不动。\n根因是**两条点 chip 的代码路径 + chip 显示解析函数三者没对齐**：\n\n- 提示词 / 公共前缀框是**权威面板**（`authoritative:true`）→ 点宫格只走 `assetRegistry.setTagBinding(\"<Audio 2>\", rel)`，\n  **只写绑定、不改正文文本**（这是对的，权威面板不该被改写）；\n- 分镜 / 时间线面板 → 走 `replaceFirstTag`，改写正文 `<Audio 2>` → `<Audio 3>`；\n- 而 chip 显示的 `tagTokenHTML()` 老实现**只对 `<Subject N>` 认绑定**，其余 tag 一律回退到\n  `fileByIndex(kind, n)` = 素材库第 N 个 → 于是权威路径下你选了 A，chip 永远显示素材库第 N 个 B。\n\n修法：`tagTokenHTML()` 改为**绑定优先、素材库回退**（① tagBindings 手动绑定 → ② `fileByIndex` 第 N 个 → ③ 抽象 glyph）。\n另外给 `render(force)` 加形参：程序化改完文本后强制重渲染，绕开「编辑中不重渲染」守卫\n（该守卫本意只是别打断用户打字，不该拦\"我们自己刚改完必须刷新\"）。\n\n**② 补上真正的回归测试（Node + playwright-core，真鼠标点击）**\n\n之前这类 bug 测不出来，因为合成 `MouseEvent` 不产生真实焦点，`document.activeElement` 判据失真 →\n**bug 版也能全绿（假阳性）**。新测试基座 `web/js/_e2e_mention_harness.html` 单独一页（避开主页 overlay 抢点击），\n提供三个 boot 变体：**裸编辑器 / 分镜面板拓扑 / 权威前缀面板**，三个 e2e 用真实 Chrome 真点真断言：\n\n- `mention_replace_e2e.cjs` **16 项**（裸编辑器 + 真实鼠标）\n- `mention_panel_e2e.cjs` **14 项**（复刻 shots.js 的 wrap + 「焦点在面板内跳过重建」订阅）\n- `mention_authbind_e2e.cjs` **11 项**（权威路径：断言绑定写入、正文未被改写、**chip 素材名确实变了**）\n\n已接入 `.mmx_tests/run_all.sh` 新增的「Node / playwright-core」段，跑一次即可回归。\n本轮全量：**12/12 PASS**（8 离线 + `asset_match` 30 项 + 3 个 Node e2e 41 项），14 项因本机无 Python playwright 跳过。\n\n**仍未接入（如实列出）**：`v2v / rv2v / mixed` 三种 task_type、`sigmas` 外接噪声表。\n\n</details>\n\n<details>\n<summary><b>v1.11.7（点开看变化）</b></summary>\n\n**① 新增「🎯 匹配素材并引用」——参考旧包算法，把引用标记真的写进正文**\n\n老包（`ComfyUI_MRBoard/director/studio_routes.py`）的「匹配引用」才是用户熟悉的那套：\n它读素材文件名当关键词，命中就在正文里**插入 `<Picture N>` / `<Audio N>` / `<Video N>`**。\n本节点以前只有「按收藏库名字匹配、只算命中不改文本」，所以\"匹配了却没引用\"。\n\n新端点 `POST /mrnext/studio/asset_match` 完整对齐老包语义：\n- **关键词 = 文件名去扩展名 + 去常见修饰后缀**（`配音 / 三视图 / 角色卡 / 场景概念图 / 概念图 / 场景图 / 场景`）\n- 支持「N=文件名」**引用表**（`1=云妙衣.png`，无序号行从 1 自动编号），编号直接成为 `<Picture N>` 的 N\n- 命中后**在关键词之后插入标记**（老包 `pos + len(keyword)` 同款），返回可直接写回 `shots[i].text` 的正文\n- 跳过正文里已存在的同类标签、遵守官方上限 **图片 9 / 音频 3 / 视频 3**\n- 返回 `unmatched`（带原因：关键词为空 / 文件不存在 / 未找到匹配），前端提示里直接显示\n\n候选来源优先级：**引用表 → 收藏库 → 素材库索引**（素材库兜底只对图片生效 ——\n否则「云妙衣配音.wav」会把角色名当关键词，在 `<Picture 1>` 旁边粘一个 `<Audio 1>`）。\n\n**② 音频参考插到「它配的那句台词」上，而不是名字旁边**\n\n老包只把 `<Audio N>` 插在关键词后，那在 H3 里是**悬空标记**（模型不知道给谁配音）。\n现在音频改插成：\n\n```\n云妙衣 <Picture 1>： 音色参考 <Audio 1>：你终于来了。\n陆沉 <Picture 3>： 音色参考 <Audio 2>：我来了。\n```\n\n这个形态正好命中 `h3prompt.py` 的 `RE_VOICE_REF_MID` → 组装成\n`(S1) says (voice reference <Audio 1>): [Chinese] 你终于来了。`\n已做**离线 round-trip 断言**（`t_match_voice.py`，6 项）：插入 → 抽出台词 → 音色编号正确、画面无残留标记。\n同一句台词只绑一个音色（两条音频不会叠在一处）；正文没有任何台词行时不插音频。\n\n**③ 修掉一个真 bug：`str.isalnum()` 把中文当字母 → 中文名匹配不上**\n\n匹配边界原来写的是 `text[st-1].isalnum()`，但**中文汉字的 `isalnum()` 也是 True** →\n「在废弃仓库门口」里的「在」被判成\"字母\" → 只有句首那一个名字能命中。\n改用 ASCII 判定（`_ASCII_ALNUM_RE`）。同一处坑在 `_scan_per_shot_refs` 的角色名匹配里\n也存在，一并修掉（这也是\"拆分后引用素材没全带过来\"的残留原因之一）。\n\n**④ 按钮分工**\n\n- **🎯 匹配素材并引用**：旧包同款，插入标记 + 写回正文 + 同步 `refMap`（主力）\n- **🔍 语义匹配**：保留旧的「按收藏名算命中、不改文本」，适合正文里已经写了角色名的场景\n\n**校验（真机 + 离线）**：新增 `asset_match_e2e` **30 项**、`t_match` **38 项**、`t_match_voice` **6 项**。\n\n**仍未接入（如实列出）**：`v2v / rv2v / mixed` 三种 task_type、`sigmas` 外接噪声表。\n\n</details>\n\n<details>\n<summary><b>v1.11.6（点开看变化）</b></summary>\n\n**① 参考不生效的真凶找到了：r2v 用错权重（fl2va ≠ ref2va）**\n\n官方两份模板用的 UNET **不是同一个**：\n\n| 官方模板 | UNET |\n|---|---|\n| `minimax_h3_director_r2v.json`（参考生视频） | `minimax_h3_ref2va_pruned_int8_convrot.safetensors` |\n| `minimax_h3_director_fl2v.json`（首尾帧） | `minimax_h3_fl2va_pruned_int8_convrot.safetensors` |\n\n而本节点原来的默认权重挑选顺序是 `fl2va → ref2va`，**r2v 永远拿到 fl2va**。\nfl2va 是「首尾帧」微调，没有被训练过参考条件 → `ref_images` / `ref_videos` / `ref_audios`\n会被当噪声**忽略**。这解释了一个反复出现的现象：构图里参考全都连上了、提示词也标了，\n出片却完全不参考 —— **因为权重根本不是参考任务的那份**。\n\n现在：\n- 后端按模式给推荐权重（`unetsByMode`：r2v → ref2va，其余 → fl2va）\n- 模型页新增「**跟随模式自动选权重（推荐）**」：切换模式自动换权重（升级前的旧配置会被自动纠正）\n- r2v + 非 ref2va 权重时，模型页给**红色告警**并附「🔧 切到 ref2va」一键修复；出片时也会提示\n- 手动选过权重即关闭自动跟随（不覆盖用户选择）\n\n**② 新增「🌐 公共提示词」（对齐官方导演台 common prompt）**\n\n- 导演台参数区新增一页：一段文字 + 「并入每个分镜（官方 `commonEnabled`）」开关 + 合并预览\n- 合并规则与官方 `concat_common_segment_prompt` 完全一致：**公共提示词 + 空行 + 本镜提示词**\n- 落地方式也照官方：r2v/i2v/fl2v 写进官方 `timeline_data.global.prompt` + `commonEnabled=true`，\n  由官方引擎逐镜拼接；t2v 单节点没有 group，由本节点按同一规则拼接\n- **公共提示词为空时绝不打开 commonEnabled** —— 否则官方会把「本镜提示词」拼两遍\n- 「出片选中」与一键流水线都会带上它（同一个参数源）\n\n**③ 顺手修：之前的告警其实用户看不到**\n\n日志栏是**单行**（`logFull.textContent = t`，后写覆盖前面的），而我上一版把\n「音色/视频标记找不到素材」「额外参考」「权重不对」等提示打在了 `▶ 第N镜` **之前** →\n**全被状态行覆盖**，等于白打。现在统一收集后与状态行合并成一条输出：\n\n```\n▶ 第1镜（参考生视频(R2V) · 5s · 图1 视0 音0） ｜ ⚠ <Audio 1> 找不到对应音频… ｜ ⚠ 当前 r2v 用的是「fl2va」…\n```\n\n**校验（真机）**：新增 `common_prompt_e2e` **18 项** 与 `ref_unet_e2e` **14 项**；\n回归 media_slots 39 / full_slots 17 / voice_payload 16 / ui_fit 21 / align_refs 14 /\nrefs_autofill 12 / timeline_refs 13 / frames 17 / assets_nodupe 11 / prod_tpl 18 / shotfmt 25 /\nfinal 26 / outflags 30 / t_split 6 / t_shotfmt 19 / t_prodtpl 32 / t_frames 8 / t_external 21 /\nt_voice 22 / t_prodtpl_api 72 **全绿（0 失败）**。\n\n**仍未接入（如实列出）**：`v2v / rv2v / mixed` 三种 task_type、`sigmas` 外接噪声表。\n\n</details>\n\n<details>\n<summary><b>v1.11.5（点开看变化）</b></summary>\n\n**槽位恢复固定 图片 9 / 视频 3 / 音频 3 —— 而且每一格都可用、都真的送进导演台**\n\n上一版为了消灭\"死格\"把格子改成了按需增长，结果槽位看不见了。这版两者都要：\n\n- **固定铺满**：图片 9 格、视频 3 格、音频 3 格（与官方参考上限一致）\n- **每一格都能用**：所有空格都渲染 `＋` 且可点（紧邻的下一格亮、后面几格淡一点，\n  悬停即点亮）—— 不再有\"点了没反应\"的死格\n- **每一格都有身份**：填充格标出它的官方编号（来自正文里真实的标记映射，不是位置序号），\n  悬停提示写明「第 N 号 · 正文里用 `<Picture N>` / `<Video N>` / `<Audio N>` 引用 · 点击预览/替换 · ✕ 移除」\n- **每一格都生效**：出片时图片 → `ref_images.ref_image_k`、视频 → `ref_videos.ref_video_k`（帧序列）、\n  音频 → `ref_audios.ref_audio_k`，跳号会压成连续槽并把提示词标记同步重编号\n- 新增一条**生效回执**：填了格子但正文里没标编号的，会提示「已作为额外参考一起送进导演台」\n  （想精确控制就写标记）\n\n**为什么点任意空格都是\"排在最后\"**：官方 presentation 对空槽**不占号**\n（`MiniMaxH3ReferenceToVideo` 用 `ref_images.values()` 走插入顺序），后端会把跳号槽位压成连续序\n并把标记同步重编号 —— 所以\"填第几格\"最终等价于\"排第几\"，按顺序追加才是诚实的映射。\n\n**真机验收**：新增 `full_slots_e2e` **17 项** —— 9 图 + 3 视频 + 3 音频**全部填满**后，\n干跑构图里 `ref_images.ref_image_0..8`、`ref_videos.ref_video_0..2`、`ref_audios.ref_audio_0..2`\n**全部连线**（这才是\"每一格都跟导演台生效\"的硬证据）；\n`media_slots_e2e` 39 项保持全绿（含\"每个空格都有 ＋\"与\"点空格真弹文件选择器\"）。\n\n回归 ui_fit 21 / align_refs 14 / refs_autofill 12 / timeline_refs 13 / frames 17 /\nassets_nodupe 11 / voice_payload 16 / prod_tpl 18 / shotfmt 25 / final 26 / outflags 30 /\nt_split 6 / t_shotfmt 19 / t_prodtpl 32 / t_frames 8 / t_external 21 / t_voice 22 /\nt_prodtpl_api 72 **全绿（0 失败）**。\n\n**仍未接入（如实列出）**：`v2v / rv2v / mixed` 三种 task_type、`sigmas` 外接噪声表。\n\n</details>\n\n<details>\n<summary><b>v1.11.4（点开看变化）</b></summary>\n\n**素材格子不再有「没用」的死格**\n\n**问题**：素材区固定铺满槽位（图片 9 格、视频/音频各 3 格），而空格的点击只挂在**「下一个空位」**上\n（`if (k === arr.length)`）。结果：\n- 图片只有 1 张时，第 3–9 格既没有图标、点了也没反应 —— 纯占位的死格\n- 视频 `0/3` 时，第 2、3 格是死的\n- 视频/音频格**连编号都没有**，看不出哪个格对应 `<Video N>` / `<Audio N>`\n\n**现在**：\n- 只渲染「**已填格 + 1 个添加位**」——不再铺满空死格（图片 1 张 = 2 格，视频 0 个 = 1 格）\n- **每个空格都有 `＋` 且可点**，点它直接弹文件选择器（图上那点\"没用\"彻底消失）\n- **每个填充格都标出它的官方编号**（`<Picture N>` / `<Video N>` / `<Audio N>`），\n  编号取自正文里真实的标记映射（不是位置序号）→ 一眼看出\"这张就是提示词里的 2 号参考\"\n- 悬停提示写明「第 N 号 · 正文里用 `<Audio N>` 引用 · 点击预览/替换 · ✕ 移除」\n- 上传框以前是**游离元素**（没插进 DOM），部分浏览器里 `click()` 不触发文件选择器 →\n  现在已挂进 DOM，\"上传没反应\"这一类问题一并消除\n\n**校验（真机）**：`media_slots_e2e` 扩到 **39 项**（含\"三种格子的数量 = 已填+1\"「每个空格都有 ＋」\n「填充格都有编号徽章」以及**点空格真的弹出文件选择器**）；\n回归 ui_fit 21 / align_refs 14 / refs_autofill 12 / timeline_refs 13 / frames 17 /\nassets_nodupe 11 / voice_payload 16 / prod_tpl 18 / shotfmt 25 / final 26 / outflags 30 /\nt_split 6 / t_shotfmt 19 / t_prodtpl 32 / t_frames 8 / t_external 21 / t_voice 22 /\nt_prodtpl_api 72 **全绿（0 失败）**。\n\n**仍未接入（如实列出）**：`v2v / rv2v / mixed` 三种 task_type、`sigmas` 外接噪声表。\n\n</details>\n\n<details>\n<summary><b>v1.11.3（点开看变化）</b></summary>\n\n**分镜块下的「视频 / 音频素材」槽：能上传、能预览、能真正被参考**\n\n**① 子目录里的素材以前根本不进素材库（根因）**\n- 视频被上传到 `<folder>/video/`、音频到 `<folder>/audio/`（与剪辑面板同一约定），但后端\n  `/mrnext/studio/files` 只扫**目录顶层** → 这些文件既不出现在素材库，也**无法被 `<Video N>` / `<Audio N>` 引用**\n- 现在 `_list_files` 同时收录 `video/`、`audio/` 一层子目录，每条带 `sub` 字段；\n  前端 `recRel / recViewUrl` 按 sub 拼 rel 与 `/view` URL —— 预览不再 404，引用也能命中\n- 素材库「素材卡」的身份/拖拽/去重键从「文件名」改成「`sub/文件名`」，同名的音频与视频不再互相串\n\n**② 参考视频真正接到官方（以前只是\"存了个路径\"）**\n- 官方 `ref_videos.ref_video_{k}` 收的是**帧序列 (IMAGE)**，不是文件路径\n- 现在 r2v 出片会把视频格的素材也发过去（`payload.video` / `videoByNum`），后端用\n  `VHS_LoadVideoPath`（没有则退回 `LoadVideoUI`）把视频解码成帧接到 `ref_videos.ref_video_{k}`；\n  **按 3 秒 / 72 帧封顶**，避免参考 token 过多把显存吃光；两个节点都没有时**明确提示并跳过**（不让整镜失败）\n- 视频与图片/音频一样走「按编号压缩 + 提示词同步重编号」，`<Video N>` 不会错位\n\n**③ 视频 / 音频的预览**\n| 位置 | 以前 | 现在 |\n|---|---|---|\n| 时间线素材小格 | 视频空白；音频只有一个小字 `♪`，点了没反应 | 视频 ffmpeg 抽首帧缩略图（失败退回 `<video>` 取帧）+「▶ 视频」角标；音频「▶ 试听」角标 |\n| 点格子 | 无反应 | 直接在灯箱里播放 / 试听 |\n| 素材库卡 | 视频是 `<video>` 元素（无角标、看不出能点）；音频点不开 | 视频缩略图 + 角标；音频可试听（灯箱新增 `<audio controls>` 支持） |\n| 收藏库卡 | 同上，音频点击只弹「去剪辑面板试听」 | 与素材库一致 |\n\n**④ 素材格还能「从素材库选」**\n- 视频 / 音频格前面多了下拉：直接选已有素材（显示 `<Video N>` / `<Audio N>` 编号），不必重新上传\n- 上传后自动刷新全局资产注册表 → 新素材**立刻**能被素材库看到、被标记引用\n\n**⑤ `<Audio N>` / `<Video N>` 跟着素材库顺序走（新）**\n- 以前「素材库 → 全部」页与「音频」页各存一份拖拽顺序，注册表用的是「全部」那份 →\n  **在音频页拖了顺序，引用标记却不跟着变**\n- 现在顺序源统一为**按类型各一份**（拖「全部」页时自动按类型拆开保存）；注册表与面板用同一份，\n  改顺序 → `<Audio N>` 徽章、时间线素材格、出片引用**全部实时同步**\n- 键兼容「带子目录」与「老格式裸文件名」两种写法，老用户以前拖好的顺序不会丢\n\n**⑥ 顺手修的两个坑**\n- 灯箱 `applyZoom` 被条件块包住导致 `ReferenceError`（音频灯箱打不开）\n- 按名字（`@名`）引用素材时**没按素材自身的 kind 归类** → 视频/音频名被塞进图片九宫格\n\n**校验（真机）**：新增 `media_slots_e2e` **29 项**（后端子目录索引 / 素材库与收藏库预览 / 时间线格子与灯箱 /\n真实出片 payload / 构图里 `ref_audios`+`ref_videos` 都连线 / 改音频顺序后引用同步）；\n回归 ui_fit 21 / assets_nodupe 11 / align_refs 14 / outflags 30 / voice_payload 16 / prod_tpl 18 /\nshotfmt 25 / final 26 / t_split 6 / t_shotfmt 19 / t_prodtpl 32 / t_frames 8 / t_external 21 /\nt_voice 22 / t_prodtpl_api 72 **全绿（0 失败）**。\n\n**仍未接入（如实列出）**：`v2v / rv2v / mixed` 三种 task_type、`sigmas` 外接噪声表。\n\n</details>\n\n<details>\n<summary><b>v1.11.2（点开看变化）</b></summary>\n\n**修「音色参考一直不被参考」—— 两个真因，都在官方对齐上**\n\n上一版已经把 `<Audio N>` 接到了官方的 `ref_audios.ref_audio_{N-1}` 槽，干跑构图里也确实有连线。\n但**连线对 ≠ 音色生效**：真正吃音色的是提示词，而提示词侧有两个错位。\n\n**① 音色标记与它要配的那句台词被切开了（主因）**\n- 模板写法是 `…嘴巴在动，音色参考 <Audio 1>：<d>[Chinese] 台词</d>`\n- 旧组装把 `<d>台词</d>` 抽成独立的 `(S1) says: …` 行，`<Audio 1>` 却留在画面描述里\n  → 送进模型的文本变成：画面段末尾一句**悬空的「音色参考 <Audio 1>：」**（后面什么都没有）\n  + 另一行不带任何标记的台词 → **参考音频与\"这句用哪个音色\"的绑定彻底丢失**\n- 现在：`<Audio N>` 被**搬进它配的那句台词行**，写成\n  `(S1) says (voice reference <Audio 1>): [Chinese] 台词`，画面里的空壳一并清掉\n- 三种模板写法都认：`音色参考 <Audio 1>：`（紧贴）、`音色参考 <Audio 1>，嘴巴零动作：`（逗号分隔）、\n  裸 `<Audio 1>：`（省略关键词）。**非配音用途**的音频参考（如 `环境音参考 <Audio 3> 是风声`）**不会**被误搬\n\n**② 官方参考槽编号是「按非空槽的先后顺序」，跳号就会错位（第二个真因）**\n- 官方 `MiniMaxH3ReferenceToVideo.execute` 里是 `for img in (ref_images or {}).values()`\n  —— 走**插入顺序**，**空槽不占号**。所以官方 UI 里槽位永远连续\n- 我们的九宫格/音频格可以跳号（只填第 2 格）。这时官方把「第 2 格」当作 **`<Audio 1>`**，\n  而提示词里写的还是 `<Audio 2>` → **标签指向不存在的参考 = 音色没给上**\n- 现在：`<Audio N>`/`<Picture N>` **按编号升序压成连续槽位**，并把提示词里的标记**同步重编号**\n  （`<Audio 2>` 单独出现 → 槽 `ref_audio_0` + 提示词改写为 `<Audio 1>`），\n  恢复「提示词第 k 号 ↔ 第 k 个非空槽」这个官方不变量；编号本就连续时行为完全不变\n\n**③ 说话人编号也跟着修了（音色不串台）**\n- 模板只在前缀声明角色（`角色 1 - 云妙衣：<Picture 1> …`），正文用 `<Picture N>` 指代。以前认不出，\n  `<d>` 的说话人只能沿用上一位 → **多角色镜头的台词全归 S1**（两条不同音色的台词变成同一个人说）\n- 现在新增「前缀 `<Picture N>` → 角色名 / S 编号」映射表；并识别镜头行里的\n  `[2 人说话：S1、S3]` 作者指定编号，两句话各归各的 S 号\n\n**④ 前端回执（标了就一定看得见有没有生效）**\n- 非 R2V 模式却写了 `<Audio N>` → 出片时黄字提醒「官方只有 R2V 支持音色参考」\n  （其他模式静默丢弃，用户以为生效了 —— 这也是「音色不被参考」的常见来源）\n- `<Audio N>` 找不到对应音频 → 提醒第几号缺、怎么补\n- 「🔍 预览本镜提示词」的回显里带上 `谁说的 (S1)（音色参考 <Audio 1>）［内心独白］`\n\n**校验（真机）**：新增 `t_voice` **18 项** + `voice_payload_e2e` **16 项**（前端真发 payload → 后端构图 →\n提示词组装全链，含跳号压缩/重编号专项）；`t_prodtpl_api` 72 项、`t_prodtpl` 32 项；\n回归 ui_fit 21 / align_refs 14 / prod_tpl 18 / assets_nodupe 11 / shotfmt 25 / outflags 30 / final 26\n/ t_split 6 / t_shotfmt 19 / t_frames 8 / t_external 21 **全绿（0 失败）**。\n\n**仍未接入（如实列出）**：`v2v / rv2v / mixed` 三种 task_type、`sigmas` 外接噪声表。\n\n</details>\n\n<details>\n<summary><b>v1.11.1（点开看变化）</b></summary>\n\n**「导出 / 音频 / 连续性开关 + 百万像素」四项正式接入 UI（本版）**\n\n上一版（v1.11.0）已经能把这三个官方开关写进工作流，但**只有导演台能改**；本版把四个控件全部接进「一键流水线」面板，并让两个面板**双向联动**。\n\n| 控件 | 一键流水线面板（新增） | 导演台（原有，现与流水线同源） |\n|---|---|---|\n| **导出模式**（官方 `exportMode`） | `🎬 全部导出（拼接）` / `📦 分段导出（独立）` 下拉 | 工具条那个导出按钮 + 文案同步 |\n| **音频模式**（官方 `audioMode`） | `🔊 生成人声` / `🤐 完全静音` 下拉 | 「🎙️ 声音」页「音频模式」下拉 |\n| **段间连续性**（`continuityEnabled`） | 勾选框 + `5/9/22/39/56 帧` 重叠帧下拉（不勾时下拉置灰） | 「📐 采样设置」页同款 |\n| **百万像素**（官方 `ResolutionSelector`） | `MP:` 输入框（0.1–2） | 采样设置页 `H3 百万像素` + 工具条那个 `MP` 框 |\n\n**统一到 store 做唯一状态源**（`store.exportMode / continuity / continuityOverlap / audioMute / vidMP`）\n- 任一面板改动 → 另一面板立即跟着变（按钮文案、勾选态、下拉值都会回填）；导演台面板在启动时就订阅 store，所以**直接开流水线改也不会丢**\n- `mpToWH()` 算式统一收在 `core/sizes.js`（与官方 `ResolutionSelector` 同源：`W = round(aw·√(MP·1024²/(aw·ah))/32)·32`，MP 钳 0.1–2），导演台与流水线共用一份，不再各写一遍\n- 百万像素生效前提：后端会把 `output.mode` 一并写成 **`fixed`**（官方只有 `fixed` 才按 width/height 出图），否则 MP 算出来的尺寸会被 `long_edge` 模式忽略\n\n**一次性迁移兜底**：老版本这四个开关只存在导演台参数里，首次打开时若 store 还是默认值、而导演台参数里确有用户选择，则把选择灌进 store；**反之（store 已被流水线设定）不会被默认值冲掉**（这条顺序一开始写反了，实测把流水线的设定反向冲回默认，已修）。\n\n**验证（真机）**：新增 `outflags_e2e` **30 项** —— 控件齐全/默认值、流水线改→store 落值、store→导演台回填（按钮文案/MP 回填/勾选态/下拉）、导演台改→store 回落、以及**干跑 `/mrnext/h3/dry` 拿构图断言** `output.mode=fixed / megapixels=1 / exportMode=segments / audioMode=mute / continuityEnabled=true / continuityOverlapFrames=22 / width×height=MP 算出的尺寸` 全部落位；\n回归：ui_fit 21 / align_refs 14 / prod_tpl 18 / assets_nodupe 11 / shotfmt 25 / final 26 / t_split 6 / t_shotfmt 19 / t_prodtpl 31 / t_frames 8 / t_external 21 / t_prodtpl_api 69 **全绿**（0 失败）。\n\n**仍未接入（如实列出）**：`v2v / rv2v / mixed` 三种 task_type、`sigmas` 外接噪声表。\n\n</details>\n\n<details>\n<summary><b>v1.11.0（点开看变化）</b></summary>\n\n**① 参考槽「按编号」接线 + 官方导出/音频/连续性开关落地（本版）**\n\n**a) 「标记了参考图/音频参考却不作用」——真因是**按列表顺序**接线，而官方是**按编号**\n- 官方参考槽的语义（`vendor/lib/ref_images.py` 的 tooltip 原文）：**`ref_image_{k}` ↔ `<Picture {k+1}>`**、`ref_audio_{k}` ↔ `<Audio {k+1}>` —— 第 N 号必须落在槽 N−1\n- 我们以前按\"顺序第 1/2/3 个\"接：正文标记 `<Picture 1>` + `<Picture 3>` → 接到槽 0/1，**「第 3 张」永远进不了图**（实测：格子里是 `[图1, 图3, …]`，接出来 `ref_image_0=图1 / ref_image_1=图3`，槽 2 空着 → 参考图等于没用上）\n- 现在：前端把「标记编号 → rel」一并发给后端，后端**按编号接**（第 N 号 → 槽 N−1）；没写编号的（按素材名自动带入的）补到**空槽位**、不抢占已编号的；旧缓存缺编号时回退顺序接法\n- 音色同理：只标 `<Audio 2>` → 只接 `ref_audio_1`，**不会把声音串到别人身上**\n\n**b) 官方三个开关以前根本没写进工作流（改了也不生效）**\n| 官方开关 | 对应 UI | 现在 |\n|---|---|---|\n| `output.exportMode`（all / segments） | 「导出方式」 | ✅ 写入 |\n| `output.audioMode`（generate / source / mute） | 「声音」页静音模式 | ✅ 写入 |\n| `output.continuityEnabled` + `continuityOverlapFrames`（5/9/22/39/56） | **新增「段间连续性」开关 + 重叠帧下拉** | ✅ 写入（官方那套\"段间重叠帧\"接镜以前永远没启用） |\n\n**c) 与官方仍有差距的地方（如实列出，不假装支持）**\n- `task_type`：官方还有 **v2v（源视频改视频）/ rv2v（参考素材改视频）/ mixed（每段自选模式）** —— 需要源视频时间轴与逐段模式，本节点**暂未接入**（已在「模式」页注明）\n- `sigmas` 输入：官方可外接噪声表（BasicScheduler 覆盖步数与调度器）—— 本节点未接\n- refine（二采）：官方 `MiniMaxH3DirectorRefine` 我们已用（二采/超分）✅\n\n校验：新参考槽按编号 5 项 + 官方开关 5 项，HTTP 全链 **69 项**全绿；回归 frames 17 / prod_tpl 18 / shotfmt 25 / ui_fit 21 / final 26 / align_refs 14 / assets_nodupe 11 / refs_autofill 12 全绿\n\n**② 采样器/调度器与参数范围全面对齐官方导演台（本版）**\n- **采样器 4 → 63 项、调度器 4 → 12 项**：官方 `MiniMaxH3Director` 的 `sampler`/`scheduler` 就是直接喂 `comfy.samplers.KSampler.SAMPLERS / SCHEDULERS`（ComfyUI 全量清单）。我们之前硬编码了 4+4 → 现在**同源取全量**，以后装了新采样器也会自动出现在下拉里\n- **逐项对齐官方 INPUT_TYPES 的默认值/范围**：steps 1–200（默认 25，改成自由输入）、cfg 0–30（默认 1.0）、frame_rate 1–240（默认 24，改成自由输入）、width/height 32–8192（step 32）、ref_max_size 32–8192（默认 864）、shift_video/audio 0.01–100（默认 12/3）；官方默认采样组合仍是 **res_multistep + simple**\n- `/mrnext/editor/options` 额外返回 **`official`**（官方默认值/范围/task_type 清单），前端据此标 min/max，避免再出现\"和官方不一样\"\n- **如实标注差异**：官方 task_type 还有 **v2v（源视频改视频）/ rv2v（参考素材改视频）**，这两个需要源视频时间轴（`<Video 1>` + `ref_videos` / `ref_video_audios` 输入），本节点暂未接入 —— 已在「模式」页写明，不假装支持\n- 校验：新增 22 项断言（清单条数 63/12、关键采样器都在、官方默认值/范围、以及**非默认参数真的写进官方节点**：`sampler=dpmpp_2m / scheduler=karras / steps=30 / cfg=1.5 / ref_max_size=1024 / shift 8,4`）；回归 frames 17 / prod_tpl 18 / shotfmt 25 / ui_fit 21 / final 26 全绿\n\n**③ 修「标记叠一起看不清」＋ 打通「音色参考」＋ 新增「H3 官方百万像素」直填（本版）**\n\n**a) 标记叠层（截图里那种\"两层 chip 糊在一起\"）**\n- 根因：渲染分两趟替换 —— ① 把 `<Picture 1>` 换成 chip HTML；② \"素材名命中\"又扫全文，在**刚生成的 chip 文本里**再包一层 token → 外层 `01_云妙衣.png`(139px) 里嵌着内层 `云妙衣`(64px)，实测一屏 8 处重叠\n- 修法：**先占位、最后统一还原** + 长名优先（短的不会吃掉长名一段）→ 重叠数 8 → **0**\n\n**b) 音色参考（`<Audio N>`）此前整条链是断的**\n- 三处各断一段：前端出片 payload 只发参考图（**没发音频**）／后端 `h3shot` 完全没有 `ref_audios` 接线／提示词又把 `<Audio N>` 换成了人名\n- 现在：r2v 出片把音频小格的素材一起发 → 构图挂 `LoadAudio` → 连到官方组节点的 `ref_audios.ref_audio_{0,1,2}`（已用干跑构图验证：图里确实出现 `LoadAudio` 与 `ref_audios.ref_audio_0` 连线）\n- **提示词恢复保留 `<Picture N>`/`<Audio N>`**：查旧包官方 tooltip，`ref_image_k` 的说明原文就是「Reference image for `<Picture {k+1}>`」，`<Audio N>` 对应 `ref_audio_{N-1}` —— **这些标记就是 H3 绑定参考槽的指针**。上一版我按\"标记会稀释注意力\"把它们换成人名，等于把绑定删了，本版回退\n\n**c) 视频分辨率新增「H3 官方百万像素」直填（0.1–2）**\n- 分辨率旁边多了个 **`MP`** 输入框：填 0.1–2 → 用**官方同一算式**算出宽高（`W = round(aw·√(MP·1024²/(aw·ah))/32)·32`，按当前比例、32 对齐）\n- 同时写进工作流 `timeline_data.output.megapixels` 并把 `output.mode` 设为 `fixed`（官方按 fixed 用 width/height 出图）→ **真的生效**：0.5MP/1.5MP 实测像素数与目标一致，越界值由后端按官方钳制（0.1–16）\n- 选预设档位或手改宽高会自动退出 MP 模式（避免两套来源打架）；前端没算到时后端也会兜底换算\n\n**d) 附带修**：`/mrnext/h3/dry` 干跑没把 `opts` 传下去 —— 校验的不是\"真正要跑的那张图\"\n\n校验：离线 31 项 + HTTP 全链 32 项（含 MP 生效 7 项、音频接线 4 项）+ e2e 全量回归 **270 项**全绿\n\n**④ 修「素材库上传后网格出现重复图」（每几秒自己多出一遍）（本版）**\n症状：上传几张素材后，网格里同一批文件出现两遍（两个 `<Picture 1>`），而且**等一会又多一层**。\n- **根因（两条刷新并发）**：`assetRegistry.refresh()` 结束时会通知所有订阅面板刷新，而 4 秒轮询 tick 紧跟其后**又调一次**面板刷新 → 两次 `refresh()` 各自先清空网格、再各自等接口、回来各自追加一遍 → 同一批素材渲染两遍\n- **修法**：给素材库面板加**并发守卫**（正在刷时只记一次\"稍后再刷\"，本轮结束补刷一次，既不丢刷新也不重复渲染）；把\"清空网格\"挪到接口返回**之后**（谁最后渲染谁说了算）；再加同 rel 兜底去重\n- 说明：**剪辑面板早就用同一套守卫**（`_refreshing`，注释写着\"并发触发导致素材重复\"），素材库面板一直漏了；这次按同一模式补齐\n- 校验：真浏览器 e2e **11 项**（3 个素材 → 连续 3 轮并发通知、真等**两个** 4 秒轮询周期、切走再切回：卡片数恒为 3，编号恰好 `<Picture 1..3>` 各一次）\n\n**⑤ 新增「🎯 对齐引用」：一键把 `<Picture N>` 对上素材库真实序号（本版）**\n短剧模板里 `<Picture 3>` 这类编号是**作者手写的**，和素材库实际排序（含你拖拽过的顺序）常常对不上 → 格子里的图就跟错。\n- 剧本面板新增 **「🎯 对齐引用」**（在「拆分分镜」右边）：读剧本/前缀里的 `**【本镜出场角色】**` 与 `角色 1 - 名字：<Picture K>` → 按名字找到素材库里的那张 → **把编号改成真实序号**，然后**自动重拆一次**，格子里的图立刻对上\n- 只改文字（剧本 + 公共前缀），**不动任何磁盘文件**：素材库序号 = 按 kind 编号 + 你的自定义排序，靠\"改文件名凑序号\"反而会和排序打架\n- 找素材的匹配顺序：手动绑定 → 收藏库/同名文件 → **文件名包含角色名**（`01_云妙衣.png`、`云妙衣_三视图.png` 都认）\n- 没生成素材的角色**保持原样不动**，并提示\"X 个还没素材，生成/收藏后再点一次\"；重复点击幂等\n- 校验：真浏览器 e2e 14 项（故意把 `云妙衣` 写成 `<Picture 6>`、`春桃` 写成 `<Picture 9>` → 点一下变成 `<Picture 1>`/`<Picture 2>`，重拆后 refMap 指向真实文件，无素材的角色不动，再点一次无副作用）\n\n**⑥ 全面对齐「生产模板」分镜格式：内置模板 / 出片提示词 / 资产图 一条链打通（本版）**\n你给的两份内置 skills 模板（`<Style 全局>`+`<Subject N>` 绑定式、`**详细描述：**`+逐镜结构化式）现在**从切镜到出片全认**：\n- **切镜 + 时长**：`【镜头 N】` + `[时长 N 秒]` → 每镜时长直接落位（8/8/9/8/8），`00:08.000，` 行首时间码自动剥掉\n- **资产（生成设定图）**：角色写在每镜 `**【本镜出场角色】**` 里（`<Picture 1> 云妙衣（S1）：高盘发…`）——现在**自动抽成 2 个角色资产**，并转成公共前缀行 `角色 1 - 云妙衣：<Picture 1> 高盘发…`，所以 **「🔗 一键流水线」和「🎨 生成设定图」不用改代码就能直接用**；设定图提示词里的 `<Picture N>` 标记会被剥掉（不再生成\"写着 Picture 1 的图\"）\n- **出片提示词（H3 三段式）**：\n  - `**【本镜音效】**` 六项（卡通特效音 / 角色动作音 / 道具音 / 液体·分泌物音 / 环境音 / 人声拟音）→ 进 `overall_soundscape`，**不再污染画面描述**\n  - `**【本镜站位】**` → 进画面描述（多角色位置，画面不乱跑）\n  - `<d>[Chinese] 台词</d>` → 按**最近的 `<Picture N>`** 归属到具体角色，并沿用作者写的 **(S1)/(S3) 编号**（跨镜不串台、音色不打架）\n  - **内心独白**（`[1 人说话：S1，内心独白]` / \"嘴巴零动作\"）→ 自动写成 `内心独白（嘴巴紧闭、零嘴部动作，只用眼神与眉毛演戏）`，不再让人物张嘴\n  - 画面里的 `<Picture N>` 一律**换成人名**（H3 不认识这些标记，留着只会稀释注意力）\n- **定义头**：`**详细描述：**` 识别为全局风格与硬规则（以前会报\"未被识别\"）\n- 顺带修三个连带 bug：①资产计划把「角色动作音：…」误判成角色；②`<Picture N>` 用非锚定 `search` 解析，从前缀行 `角色 1 - 名字：<Picture 1> …` 里又切出一个假角色（出现\"高盘发垂长发\"这种角色名）；③风格行含「色调」二字被截断\n- **内置模板重写**：「一键流水线示范」改成生产模板格式（逐镜角色/站位/音效 + `<d>` 台词 + 内心独白示例），并新增「古风宫廷喜剧（生产模板 · 完整示例）」\n- 校验：新增离线 30 项（提示词结构）+ HTTP 全链 21 项 + 真浏览器 e2e 18 项（真点「拆分分镜」→ 5 镜/时长/前缀角色/分镜卡/前端解析器 2 角色 + 2 张设定图任务 + 三段式）；回归 249 项全绿（共 **318** 项）\n\n**⑦ 支持「时间码分镜」剧本：`**【00:00 – 00:08】镜头一｜全景→中景**`（本版）**\n短剧 / 网文改编常见的写法，现在**切镜头 + 抽资产**全通：\n- **镜头边界**：识别 `**【00:00 – 00:08】镜头一｜全景→中景**`（也兼容 `【00:00-00:08】镜头1`、`[00:00 – 00:08] 镜头一`、无括号的 `00:00–00:08 镜头一`）→ 切出 6 镜，**每镜时长直接取时间码差值**（00:00–00:08 → 8s、00:08–00:18 → 10s…），不再需要每镜手写「5秒」\n- **中文数字镜号**：`镜头一 / 镜头二 …`、`第一镜 / 第二镜` 也能切（以前只认阿拉伯数字）\n- **定义头（资产）**：`<Style 全局>` → 全局风格；`<Subject 1> 女主·苏棠：18岁…` → 角色资产 → 「生成设定图」直接按 2 个角色出图\n- **`**【字幕卡】**`** 只剥标签、保留卡上文字：H3 规则里双引号文本 = 画内字幕，正好就是字幕卡的语义（不会把\"字幕卡\"三个字当成画面描述喂给模型）\n- **`---` 分隔线静默跳过**（以前会在公共前缀里报一条「未被识别」告警）\n- 顺带修一个老 bug：**风格行里只要出现「色调」两字就会被从中间截断**（「明亮暖色调，柔光滤镜…」→ 只剩「，柔光滤镜…」）；现在关键词必须\"行首 + 冒号\"才算键值对\n- 「📄 剧本模板」里新增 **⏱ 时间码分镜模板**，点「使用」即可套用\n- 校验：离线单测 19 项 + 真浏览器 e2e 25 项（含真点「拆分分镜」按钮后的 6 镜 / 时长 / 公共前缀 / 分镜卡，以及前端解析器抽出的 2 角色 + 完整风格）\n\n**⑧ 修「时间线『全部导出』按钮有时候不显示，点一下才出来」+「剪辑面板轨道素材一多就被裁切」（本版）**\n- **导出方式按钮（🎬 全部导出 / 📦 分段导出）首屏就是空的**：它创建时不带文案、只在\"被点击\"时才写入 → 第一次进「🎬 时间线」看到的是一个 **126px 宽但只有 11px 高的空条**（视觉上就是\"按钮没显示\"），**点一下才冒出文字**。现在构造后立刻刷新 + 每次渲染都同步（从旧计划/存档恢复 `exportMode` 也会跟着变）\n- 同一行的 **「（仅一采）」提示原来是写死的**，把「二采」下拉切成「自动二采」它也不变 → 现在随下拉实时刷新\n- **剪辑面板：素材一多，末尾片段被顶出轨道可视区（看着像被裁切）+ 标尺不跟滚动**：以前时间轴比例固定 `30px/s`，片段一多轨道就横向溢出。现在**按\"整条时长 ÷ 轨道可视宽度\"自动缩放**，素材越多比例越小、整条全览；按钮行右侧会提示 **「已自适应缩放 x px/s，整条 xxs 全览」**。多到实在看不清时（平均片段 < 20px）才停止缩小、保留横向滚动，并让**标尺与三条轨一起滚**（刻度始终对得上片段）\n- 顺带修一个藏了很久的换算 bug：节点 DOM 面板会被画布**整体缩放**（实测 0.9），而指针事件给的是\"屏幕像素\"、面板内部用的是\"布局像素\" → 播放头点选与裁剪拖拽一直缺这个系数（缩放 90% 时\"拖 60px 只走 54px\"）。现在统一补偿，裁剪真正跟手\n- 校验：真浏览器 e2e **21 项**全绿（含\"10 个片段零横向溢出、片段全部在轨道内\"\"播放头精确落在点击处 ±3px\"\"拖 -60 屏幕像素 → 宽度减少 60px\"）\n\n**⑨ 修「自动拆分到导演台后，引用素材没全部落到下方格子」（本版）**\n三个根因一起修（用户实报：提示词里 4 个素材标记，格子只有 1 张）：\n- **`<Picture N>` 等显性标记只有\"手动绑定过\"才注入** → 现在没绑定时**按素材库序号回退**（与提示词里 token 的显示完全一致），提示词里看得见的标记，格子里就有图\n- **中文名后面紧跟中文就匹配不上**（`小白兔跳过来` 里的 `小白兔` 因右侧是汉字被判掉）→ 改成\"左边不是字母数字 + 不落在更长的已知名字里\"（`张三` 不会误吃 `张三丰`）\n- **后端只认前端传来的名字→文件映射** → 现在前端没给/给过期时后端自己解析（收藏库同名条目 → 素材库文件名主名），API 自洽\n- 附带修：**单镜剧本拆不出来**（只有 1 块时被当成\"定义头\"吃掉 → 报\"未切出分镜\"）；现在带时长的单块一律当分镜\n- 前端再加一层保险：时间线建立每镜缓存时，按正文里能看见的标记/名字**补齐**素材槽（幂等，只加不删，手动挑过的不会被覆盖；已有的分镜不用重新拆也能补上）\n- 校验：离线 6 项 + 真浏览器 e2e 12 项（含三张素材全带过来、第2镜 refMap 为空也能按正文补出）\n\n**⑩ 首尾帧生视频：素材区只有「首帧 / 尾帧」两个框（对齐旧包 · v1.10.1）**\n- 切到 **FL2V（首尾帧生视频）** 时，时间线下方素材区**只给两个上传框**：左「首帧」（绿角标，必填）、右「尾帧」（橙角标）——就是本模式全部的参考图；**不再有九宫格，也没有视频/音频小格**\n- **I2V** → 只给「首帧」一个框；**L2V（尾帧生视频）** → 只给「尾帧」一个框；**R2V** 仍是九宫格 + 音视小格；**T2V** 无素材区\n- 两个框支持：点击上传 / 「从素材库选图」下拉 / 右上角 × 清除；框体宽高比跟随当前出片分辨率（竖屏就是竖框）\n- **槽位语义严格保序**：只填尾帧时，尾帧就待在「尾帧」格，不会被挤到「首帧」格（老实现会把空槽压掉 → 三种模式跟着串）\n- **少填一张也能出片**：FL2V 只填首帧 → 自动按 I2V 出片；只填尾帧 → 自动按 L2V 出片（回执里说明）。缺对应帧时前后端都会拦住并提示，不会白等一轮\n- 校验：离线单测 8 项 + 真浏览器 e2e 17 项全绿（含\"删掉首帧后尾帧不位移\"\"i2v 只剩尾帧被拦住\"）\n\n**⑪ 外部模型节点 / 第三方加速节点接口（外接后内置加速自动失效）**\n- 「🎬 时间线」→「⚡ 加速」页新增 **外部节点（模型 / 加速）** 区块：点「🔗 扫描画布外部节点」读当前画布\n  - **外部模型节点**：UNETLoader / CheckpointLoader / GGUF / Nunchaku / CLIPLoader / VAELoader / LoraLoader …… 列出文件名 + 一键 **「采用」**（填进本节点「🧠 模型」页）；文件名不含 `minimax/h3` 会标 ⚠ 并二次确认（防止误用 SDXL 之类模型）\n  - **第三方加速节点**：SageAttention/BlockSparse、TeaCache/缓存类、torch.compile、FlashAttention、Nunchaku/torchao 量化、分块显存优化……\n- **画布上一旦出现第三方加速节点 → 内置加速自动整体失效**（BlockSparse 归位「关闭」、蒸馏 LoRA 归位「(无)」）。原因：两者都会改写模型与噪声调度，叠加 = 双重加速（画面发灰、显存反而爆）\n- 出片回执会写明 `🔗 已检测到外部加速节点（…）→ 本节点内置加速已自动失效（原本启用：…）`，让你知道内置加速为什么没生效\n- 确实想叠加 → 勾 **「强制启用内置加速（忽略外部节点）」**，界面提示\"可能重复叠加\"\n- **后端双保险**：即使前端没传，构图阶段同样让路 —— 送进队列的图绝不会同时出现内置与外部加速\n- 校验：离线单测 **21 项** + 真浏览器 e2e **19 项**全绿\n\n**⑫ 资产保存路径 + 右键导出（自动分类）+ 本地删除实时检测（v1.9.9）**\n- 「📁 保存路径」+「⬆ 从保存路径导入」；卡片右键 `⬇ 保存到本地文件夹（自动分类）` / `📂 在文件夹中显示` / `✎ 重命名` / `🗑 删除`\n- 导出按 `角色 / 场景 / 素材 / 音频` 自动建子目录，同名同大小跳过\n- 本地删除每 4 秒实时反映：列表自动移除 + 清缩略图缓存；收藏库失效条目打「文件已丢失」标 + 一键清理\n\n**⑬ 修「切分到导演台后，时间线素材区不自动加载素材」（v1.9.9）**\n- `refMap` 被 `sanitizeState` 每次 `store.set` 清空（只认平面数组，实际是每镜一组）→ 两种形态都处理\n- 新增 `store.splitStamp`：方案变更时时间线丢缓存、按最新引用重新导入；并补\"空着才自动补\"的兜底（不覆盖手动选择）\n\n**上一版 v1.9.7 / v1.9.6**\n- 素材库/收藏库 ✎ 就地改名 + 全画面板标记实时同步\n- 公共前缀识别大幅放宽；剪辑面板「丝滑裁剪」（拖拽逐帧实时跟手 + 浮层 + 快捷键）\n\n</details>\n\n---\n\n## 目录\n\n1. [能力总览](#1-能力总览)\n2. [安装流程](#2-安装流程)\n3. [模型准备（关键）](#3-模型准备关键)\n4. [验证安装成功](#4-验证安装成功)\n5. [5 分钟快速上手](#5-5-分钟快速上手)\n6. [界面总览](#6-界面总览)\n7. [完整流水线教程（6 步）](#7-完整流水线教程6-步)\n8. [出片模式对照表](#8-出片模式对照表)\n9. [放大与超分（倍率统一说明）](#9-放大与超分倍率统一说明)\n10. [生图面板 & 分镜九宫格故事板](#10-生图面板--分镜九宫格故事板)\n11. [剪辑面板（剪映式）](#11-剪辑面板剪映式)\n12. [Skill 提示词优化](#12-skill-提示词优化)\n13. [主题外观与缩放适配](#13-主题外观与缩放适配)\n14. [缓存与清理](#14-缓存与清理)\n15. [常见问题排查](#15-常见问题排查)\n16. [产物路径速查](#16-产物路径速查)\n\n---\n\n## 1. 能力总览\n\n| 能力 | 说明 |\n|---|---|\n| 🎬 五种出片模式 | t2v 文生视频 / i2v 首帧生视频 / fl2v 首尾帧 / fl2v_tail 尾帧 / r2v 参考主体 |\n| 🎙️ 音视频联合生成 | H3 官方三段式提示词，自动分配说话人 (S1)(S2)，自动生成台词与环境音 |\n| 🖼️ 生图 | 文生图 / 图生图 / 参考生图 / 编辑图（Krea2），14 条内置预设 |\n| 🧩 分镜九宫格故事板 | 一键出 3×3 故事板，**切格即 i2v 首帧**（4 套模板，竖屏直出 9:16） |\n| ✨ 放大超分 | 图片 / 视频统一「倍率」：内置精修、SeedVR2、RTX VSR、FlashVSR、**VOSR2** |\n| ✂️ 剪辑 | 剪映式时间轴：标尺、裁剪把手、变速、音量/静音、撤销重做、快捷键 |\n| 🛠️ 提示词优化 | 本地 Qwen（llama.cpp）按 Skill 规范扩写，**带推理过程预览** |\n| 🎨 主题 | 🎨默认 / ⚡蓝色流光 / 🔥火焰流动（含点击喷火），一键改主题色，全面板等比缩放 |\n| 🧰 工具 | 原生导入/导出、工作流消毒、长文档适配 |\n\n---\n\n## 2. 安装流程\n\n### 2.1 环境要求\n\n- ComfyUI **v0.3.4x 及以上**（本包在 0.34.6 实测通过；仅 nightly 修掉了「低于 8 步音频失真」的 bug）\n- Python 3.10+（便携包自带 `python_embeded` 即可）\n- 显存：**16GB 可跑** 864×480 / 608×1056 等档位；24GB+ 更从容\n- 内存：≥32GB 建议（CLIP 文本编码器 int8 版载入约 30GB）\n\n### 2.2 三步安装\n\n**步骤 1 · 放包**\n\n把 `ComfyUI_MRBoard_Next` 整个文件夹放到：\n\n```\n你的 ComfyUI 目录/\n└── custom_nodes/\n    └── ComfyUI_MRBoard_Next/     ← 放这里\n        ├── __init__.py\n        ├── nodes/    server/    web/js/\n        ├── vendor/                ← H3 出片引擎已内嵌，不用单独装\n        └── README.md              ← 本文件\n```\n\n> 便携包用户路径示例：`X:\\ComfyUI_portable_TE\\ComfyUI_portable_TE_v260619\\ComfyUI\\custom_nodes\\ComfyUI_MRBoard_Next`\n>\n> ⚠️ 一定解压成**带全部子目录的完整文件夹**。如果只拷了 `__init__.py` 或少了 `vendor/`，出片会失败。\n\n**步骤 2 · 装依赖（一次性）**\n\n便携包根目录执行：\n\n```bash\npython_embeded\\python.exe -m pip install imageio_ffmpeg Pillow opencv-python-headless \"scenedetect>=0.6.4,<0.8\"\n```\n\n| 依赖 | 作用 | 缺了会怎样 |\n|---|---|---|\n| `imageio_ffmpeg` | 剪辑合成 / 抽帧 / 缩略图 | 剪辑合成报「未找到 ffmpeg」 |\n| `Pillow` | 占位图、读取图片尺寸 | 生图占位、SeedVR2 倍率换算失效 |\n| `opencv-python-headless` | 时间线导入外部视频解码 | v2v / 外部视频导入失败 |\n| `scenedetect` | 剪辑页「智能分割」 | 智能分割按钮不可用 |\n\n**步骤 3 · 重启 ComfyUI**\n\n关掉再启动（**改了 `nodes/*.py` 或 `server/*.py` 都必须重启**；只改前端 `web/js/*` 则 Ctrl+F5 即可）。\n\n### 2.3 可选增强（按需安装）\n\n| 想用的功能 | 额外需要 |\n|---|---|\n| **VOSR2 超分**（推荐，一步扩散，快且省显存） | ① `custom_nodes/ComfyUI-VOSR2` 节点包 ② 模型放 `models/vosr2/`：（`args.json`、`checkpoints/ema_model.safetensors`、`Qwen-Image-vae-2d/`、`dinov2_vitl14*.safetensors`）。**扁平布局或 `vosr2/VOSR2/` 子目录布局都认**。装好后生图面板会自动解除「VOSR2」选项的置灰 |\n| **SeedVR2 超分** | `custom_nodes/ComfyUI-SeedVR2_VideoUpscaler`（节点 + CLI）+ `models/SEEDVR2/` 下的 DiT 与 VAE |\n| **RTX VSR / FlashVSR 超分** | 随节点包附带，需 NVIDIA 显卡（RTX VSR 走 nvvfx 硬件加速） |\n| **3D latent 二采** | `models/latent_upscale_models/minimax_h3_latent_upscaler_3d_bf16.safetensors`（⚠️ 2D 权重不可用） |\n| **提示词优化** | `comfyUI-llama-TE` 包 + `models/LLM/*.gguf`（Qwen，可带 mmproj） |\n\n### 2.4 升级 / 回滚\n\n- **升级**：直接覆盖 `custom_nodes/ComfyUI_MRBoard_Next` 整个目录 → 重启 ComfyUI → 浏览器 **Ctrl+F5**。\n- **回滚**：历史版本包都在 `custom_nodes/_packs/`（同时备份在 `Downloads`）。删掉现目录、解压旧包即可。\n- ⚠️ 不要同时保留旧包 `ComfyUI_MRBoard` 与新包：H3 节点会重复注册（功能一致，以先加载的为准）。\n\n---\n\n## 3. 模型准备（关键）\n\n所有模型放 ComfyUI 标准目录 `models/` 下，**本包会自动扫描，文件名对上就能用**。\n\n### 3.1 必备（出片）\n\n| 用途 | 目录 | 示例文件名 |\n|---|---|---|\n| H3 UNet（出片主模型） | `models/diffusion_models/` | `minimax_h3_fl2va_pruned_int8.safetensors` |\n| Qwen3VL 文本编码器 | `models/text_encoders/` | `qwen3vl_32b_minimax_h3_int8_convrot.safetensors` |\n| H3 视频 VAE | `models/vae/` | `minimax_h3_video_vae_fp16.safetensors` |\n| H3 音频 VAE | `models/vae/` | `minimax_h3_audio_vae_fp32.safetensors` |\n\n> ⚠️ **FL2VA 版模型支持全部 5 种模式**（T2V/I2V/FL2V/L2V/R2V），优先用它。\n>\n> ⚠️ **CLIP 量化档位决定\"听不听话\"**：H3 是 CFG=1.0（无负引导）模型，画面完全靠 CLIP 的语义理解。\n> 本包默认优先选 **int8 版**（保真度最高）。如果你手动换成 `*nvfp4_awq`（4bit，省内存）而发现「画面不按提示词走」，换回 int8 即可。时间线面板的「🧠 模型」页会直接标出 ⚠ / ✓。\n\n### 3.2 可选\n\n| 用途 | 目录 | 备注 |\n|---|---|---|\n| Krea2 生图模型 | `models/diffusion_models/` | 角色/场景设定图、九宫格故事板 |\n| 编辑 LoRA | `models/loras/` | 图片编辑（如 identity_edit） |\n| 加速 LoRA | `models/loras/MiniMax-H3/` | 4/8 步蒸馏；强度在 **0.5–1.0** 之间扫，别盲套 alpha=8 |\n| 本地 LLM | `models/LLM/` | Skill 提示词优化（GGUF） |\n\n**检查方法**：打开时间线面板 → 顶部模型下拉 → 应该有文件列表。空的 = 路径不对或没重启。\n\n---\n\n## 4. 验证安装成功\n\n**① 看启动日志**（出现即加载成功）：\n\n```\nMRBoard_Next vendored H3 引擎已加载：N 个节点类\n```\n\n**② 画布空白处双击 → 搜索 `MRBoard` → 添加「MR分镜助手导演台 · Next」**\n\n- 节点尺寸**锁死 1100×860**（故意设计，面板内容多）；可以手动拉大，UI 会等比放大\n- 节点上**不会出现画布预览大图**（已主动屏蔽，避免撑高节点）\n\n**③ 看版本徽章**（判断浏览器有没有加载到新版前端）\n\nF12 控制台执行：\n\n```js\ndocument.querySelector('[data-mrnext-version]').getAttribute('data-mrnext-version')\n// → \"1.9.4\"\n```\n\n显示旧版本号 → **Ctrl+F5 硬刷新**（浏览器缓存了旧 JS）。\n\n---\n\n## 5. 5 分钟快速上手\n\n最短路径跑出第一段视频：\n\n1. 点左侧 **📜 剧本** → 粘贴剧本 → 点「拆分分镜 → 自动时长 → 切分到导演台」\n2. 点 **🎬 时间线** → 顶部「🎬 模式」选 **文生视频（纯文字）**\n3. 在第一个镜头的提示词框里写好画面描述\n4. 点该镜头的「出片」→ 等 1–3 分钟\n5. 右侧「实时预览」框会实时显示采样画面，出片后原地变成视频预览\n\n剧本模板：\n\n```\n【分镜1】\n5秒 雨夜老街，红衣女子撑伞快步走过，镜头跟随推近，霓虹倒影在水面晃动。\n\n【分镜2】\n5秒 她停在路灯下回头，雨水顺着伞骨滑落，特写她的眼睛。\n```\n\n> 直接粘分镜、不写任何标记也能跑（纯 t2v 不需要素材）。\n\n---\n\n## 6. 界面总览\n\n**左侧导航（10 个面板）**\n\n| 面板 | 作用 |\n|---|---|\n| 📜 剧本 | 写/粘贴剧本、抽公共前缀、拆分分镜、生设定图 |\n| ⚙️ 流水线 | 一键全自动（拆分→出图→出片→合成） |\n| 🎞️ 分镜 | 逐镜编辑正文、按素材匹配引用 |\n| 🗂️ 素材库 | 浏览 / 上传 / 收藏入册 |\n| ⭐ 收藏库 | 名字 → 文件映射（`<Picture 1>` 等标记靠它解析） |\n| 🎨 生图 | Krea2 生图 + 画质增强 + 九宫格故事板 |\n| 🛠️ Skill 优化 | 本地 Qwen 提示词扩写（含推理过程预览） |\n| 🎬 时间线 | 逐镜出片（5 模式）、二采放大、整条连跑 |\n| ✂️ 剪辑 | 剪映式序列合成 |\n| 🧰 工具 | 原生导入导出、工作流消毒、长文档适配 |\n\n**顶部栏**：6 个预设主题色圆点 + 自定义取色器 + 主题切换按钮（🎨默认 → ⚡蓝色流光 → 🔥火焰流动）\n**底部**：运行日志（出片日志会回显「实际送模型的提示词」，可自查是否被污染）\n\n### 6.1 素材/收藏的「保存路径 · 右键导出 · 本地删除检测」\n\n**📁 保存路径**（素材库 / 收藏库顶部）：点它选一个本机文件夹（跨面板/跨会话记住）。用途：\n\n| 操作 | 说明 |\n|---|---|\n| 卡片上 **右键** → `⬇ 保存到本地文件夹（自动分类）` | 把该资产另存到你选的目录，并按分类自动建子目录：`角色 / 场景 / 素材 / 音频`（有收藏就按收藏分类，没有就按扩展名）。同名同大小会自动跳过，不会堆一堆 `_2 _3` |\n| 卡片上 **右键** → `📂 在文件夹中显示` | 打开系统文件管理器并定位到该文件 |\n| 顶部 **⬆ 从保存路径导入** | 把保存路径里的图片/音频/视频（含子目录）一键导回资产文件夹 → 导入后立刻能在剧本/时间线里引用 |\n\n**本地删除会实时反映**（不用重启、不用手动刷新）：素材库/收藏库挂载期间每 4 秒轻量重扫一次 ——\n\n- 文件被外部删掉 → 素材库列表里自动消失，它的缩略图缓存也被清掉\n- 收藏库里对应的条目 → 卡片打上红框「文件已丢失」，头部提示 `⚠ N 条文件已丢失`，点 **「🧹 清理失效收藏」** 一次性收拾干净\n\n> 时间线 / 剪辑面板的素材区也会跟着重扫（切过去就是最新的）。\n\n### 6.2 外部模型节点 / 第三方加速节点（内置加速自动让路）\n\n「🎬 时间线」→「⚡ 加速」页最上方有 **「外部节点（模型 / 加速）」** 区块：\n\n1. 点 **「🔗 扫描画布外部节点」**，列出当前画布上的：\n   - **外部模型节点**：`UNETLoader` / `CheckpointLoader(Simple)` / GGUF / Nunchaku / `CLIPLoader` / `VAELoader` / `LoraLoader` 等 → 显示它选的文件名，点 **「采用」** 直接填进本节点「🧠 模型」页\n     - 文件名里没有 `minimax` / `h3` 时会加 ⚠ 并在采用前二次确认（避免误把 SDXL 之类的模型当成 H3 模型）\n   - **第三方加速节点**：SageAttention / BlockSparse、TeaCache 等缓存加速、`torch.compile`、FlashAttention、Nunchaku / torchao 量化、分块显存优化……\n2. **只要画布上出现第三方加速节点，本节点的内置加速会自动整体失效**：`BlockSparse 加速` → 关闭、`蒸馏 LoRA` → (无)。\n   原因：内置与外部加速都会改写模型 / 噪声调度，叠加就是「双重加速」——画面发灰、显存反而爆。\n3. 出片时底部日志会回执：`🔗 已检测到外部加速节点（…）→ 本节点内置加速已自动失效（原本启用：…）`，避免你疑惑\"内置加速怎么没生效\"。\n4. 确实想叠加 → 勾 **「强制启用内置加速（忽略外部节点）」**，界面会提示\"可能与外部加速重复叠加\"。\n\n> 后端也做了兜底：即使前端没传标记，构图阶段同样会让路 —— **送进队列的图里绝不会同时出现内置加速节点和外部加速**。\n\n### 6.3 改名字（素材库 / 收藏库）· 一次点击全局同步\n\n两个库的每张卡片 hover 后都多了一个 **✎** 按钮，点它名字会**就地变成输入框**（`回车`/点别处 = 确定，`Esc` = 取消）：\n\n| 在哪个面板点 ✎ | 改的是什么 | 说明 |\n|---|---|---|\n| 🗂️ **素材库** | **磁盘文件名** | 保留扩展名；重名自动加 `_2`；不改内容、不覆盖已有文件 |\n| ⭐ **收藏库** | **收藏名**（剧本里引用的名字） | 收藏名和文件名一致时会问一句「要不要连磁盘文件一起改」（确定 = 一起改，保持一致）；两者本来就不同名 → 只改收藏名 |\n\n**改完立刻自动同步（不用刷新页面、不用重新拆剧本）**：\n\n- 收藏库里指向该文件的条目 → `rel` / 名字一起更新（不会再出现\"收藏还在、图打不开\"）\n- 公共前缀的 `<Picture N>` / `<Subject N>` 手动绑定、按名字的绑定 → rel 换到新文件\n- `store.refMap`（每镜素材引用）、素材库自定义拖拽顺序 → 一起跟着改\n- **剧本 / 公共前缀 / 每镜正文里的旧名字会自动改写为新名字**（标记不会\"掉\"），如果还出现了别的更长素材名（如 `林晚` 与 `林晚B`）会自动保护、不会被改坏\n- 所有面板里已经打开的素材标记（token）与缩略图 → 实时指向新文件；时间线/分镜/剪辑的素材区也会重扫\n- 缩略图缓存清掉旧 key，避免还挂着改名前的旧图\n\n---\n\n## 7. 完整流水线教程（6 步）\n\n### 第 1 步 · 写剧本 📜\n\n支持两种写法，**可以混用**：\n\n```\n<Subject 1> 小白兔\n描述：雪白绒毛，红眼睛，长耳朵\n<Picture 1> 森林草地场景\n<Style 全局> 明亮卡通风格，柔和水彩质感\n\n【分镜1】\n5秒 森林草地上，小白兔蹦蹦跳跳摘下一朵野花。\n【分镜2】\n4秒 特写小白兔的耳朵竖起，警觉地看向远处。\n```\n\n- `【分镜N】` 或 `分镜N` 都认；时长写在正文里（`5秒`/`5s`）会自动提取\n- **公共前缀**（`<Subject N>` / `<Picture N>` / `<Style 全局>` / 「角色 1 - 名字」）用「📦 自动抽公共前缀」一键抽出，之后会自动注入每个分镜\n- 点「拆分分镜 → 自动时长 → 切分到导演台」，分镜自动进入 🎬 时间线\n\n#### 公共前缀支持的写法（v1.9.6 大幅放宽）\n\n公共前缀里**只要能被认出来的角色/场景就会各生成一张设定图**，每行 1 个条目。以下写法全部支持，可混用：\n\n| 写法 | 例子 |\n|---|---|\n| 标签同行 | `<Subject 1> 林晚：28岁女性，及肩黑发` |\n| 标签 + 逗号 | `<Subject 1> 林晚，28岁女性，及肩黑发` |\n| 标签独占一行 | `<Subject 2>` ⏎ `名称：陆沉` ⏎ `描述：32岁男性，深灰风衣` |\n| 标签独占一行 + 裸名 | `<Picture 1>` ⏎ `雨夜街头：霓虹倒影` |\n| 序号角色行 | `角色1：林晚，28岁女性` ／ `场景二：雨夜街头，霓虹倒影` |\n| 破折号写法 | `角色 1 - 张老汉：满脸皱纹` ／ `场景 1 - 破庙：残破的庙宇` |\n| 小标题 + 列表 | `【角色】` ⏎ `- 林晚：28岁女性` ⏎ `1. 陆沉：32岁男性` |\n| Markdown 小标题 | `## 一、人物` ／ `**场景设定**`（粗体、`#`、`-`/`*` 列表符都会自动剥掉） |\n| 括号描述 | `- 林晚（28岁女性，及肩黑发）` |\n| 裸键值（无小标题） | `林晚：28岁女性`（默认当角色；名字像地点如「雨夜街头」自动判为场景） |\n| 全局风格 | `<Style 全局> 电影级写实，冷色调` ／ `<Style 全局>` ⏎ `冷色调` ／ `风格：冷色调` |\n\n- 名字里的序号、`【】`、`**`、反引号都会自动清理；`角色之间的关系：…` 这类属性说明行不会被误当成角色\n- 面板会实时显示 `识别到 N 角色 / M 场景`，鼠标悬停可看到**具体识别出了哪些名字 + 哪几行没认出来**；点「👁 预览」可先看清每条会喂给模型的 prompt\n- 只有一条都认不出来时才会提示「没识别到」——此时把提示里那几行原文按上表改一下即可\n\n### 第 2 步 · 分镜面板 🎞️（可选微调）\n\n逐镜查看正文、改字、看每镜的时长与帧数、按素材匹配引用。\n\n### 第 3 步 · 生成设定图 🎨（可选，但强烈建议）\n\n角色形象固定下来，跨镜一致性会好很多：\n\n1. 📜 剧本页点「⚡ 生成设定图并入收藏」（或去 🎨 生图页手动生成）\n2. 生成结果进 ⭐ 收藏库\n3. 剧本里写 `<Picture 1> 小白兔`，出片时会自动带上对应图\n\n### 第 4 步 · 时间线逐镜出片 🎬\n\n> 💡 **切分到导演台后，本镜素材会自动进九宫格（全部，不只是命中的那几张）**：\n> 面板按 `store.refMap`（拆分的每镜引用）填左侧素材区，并且会**按正文里能看见的标记/名字再补一轮**——\n> `<Picture N>`/`<Subject N>` 取素材库第 N 张（或手动绑定），正文里直接写名字的按收藏库/素材库同名解析。\n> 若你手动挑过素材，自动补全**不会**覆盖你的选择；想重新按引用灌一遍，点该镜右上角「重置」。\n\n\n顶部「🎬 模式」选任务类型（见第 8 节），然后在每个镜头块里：\n\n| 控件 | 说明 |\n|---|---|\n| 素材九宫格 | i2v/fl2v/r2v 用；点格子选素材。**t2v 下会显示「⚠ 本模式不使用素材」**，明确告诉你图不会被用 |\n| 提示词框 | 画面描述；支持 `@收藏名` 高亮为素材标记 |\n| 实时预览 | 固定方框：生成中显示采样预览，出片后变视频（可播放/暂停），手动二采模式下带「✨ 二采高清放大」 |\n| 秒 | 单镜时长（顶部开了「统一全局时长」时禁用） |\n| 衔接下镜 | 勾选后把上一镜结尾写进本镜提示词，保持连贯 |\n| 出片 / 复制 / 重置 / 删除 | 单镜操作 |\n\n**整条连跑**：顶部「▶ 整条连跑」按顺序把所有镜头跑完（可勾「段间清理显存（每2镜）」防长时间连跑 OOM）。\n\n**出片后必看日志**：会打印「实际送模型的提示词」，能一眼看出提示词有没有被污染、三段式结构对不对。\n\n#### 🎙️ 声音 / 台词（治「说话乱说」）\n\n点开「🎙️ 声音」页。H3 是**音视频联合生成**模型，不写声音字段它就会自己补人声 → 表现为\"张嘴说胡话\"。本包自动按官方三段式组装：\n\n```\nintegrated_multimodal_description:  画面 + 动作 + 镜头 + 说话人 (S1) + 台词 [Chinese] xxx\noverall_soundscape:                 环境音（不含对白）\nnon_diegetic_music:                 画外配乐（或 N/A）\n```\n\n三条硬规则（本包已自动处理，知道就行）：\n\n- 台词**只能用** `[Chinese] 台词` 这种写法，**不能用双引号**（双引号在 H3 里是\"画面上可见的字\"，会渲染成字幕）\n- 说话人 (S1)/(S2) 由公共前缀角色顺序分配，**跨镜头天然一致**，不要手动乱编号\n- **无台词必须显式声明**，否则模型照样安排人说话\n\n#### 📐 画质与步数\n\n| 档位 | 步数 | 采样器 | 说明 |\n|---|---|---|---|\n| 草稿 | 6 | res_multistep / simple | 需配蒸馏 LoRA，强度 0.5–1.0 |\n| 标准（推荐） | 12 | res_multistep / simple | cfg=1、shift 保持训练值（视频 12 / 音频 3） |\n| 成品 | 20 | res_multistep / simple | 出片后自动二采 |\n\n> ⚠️ **低于 8 步音频会失真** —— 这是 ComfyUI 稳定版的 bug（commit `bdcb886`，仅 nightly 修复）。\n> 本包内置**低步数音频护栏**：步数低于安全线会自动抬到 8 步并在日志回执。\n> 想真跑 4–6 步：把 ComfyUI 升到 nightly，再到「🎙️ 声音」页调低安全线/关掉护栏。\n\n### 第 5 步 · 二采放大（可选）\n\n见第 9 节。\n\n### 第 6 步 · 剪辑成片 ✂️\n\n见第 11 节。\n\n---\n\n## 8. 出片模式对照表\n\n| 模式 | 需要素材 | 素材区形态 | 构图（自动） | 用途 |\n|---|---|---|---|---|\n| **t2v** 文生视频 | 不需要 | 无素材区 | 单个 Director，**零素材节点** | 纯文字描述出片 |\n| **i2v** 首帧生视频 | 1 张图 | **1 个框：首帧** | 1× LoadImage + GroupImageToVideo | 用设定图作为起始帧 |\n| **fl2v** 首尾帧 | 1–2 张图 | **2 个框：首帧 / 尾帧** | 2× LoadImage | 指定起点与终点，中间自然过渡 |\n| **fl2v_tail** 尾帧 | 1 张图（尾帧） | **1 个框：尾帧** | 1× LoadImage | 反推走到该定格帧 |\n| **r2v** 参考主体 | N 张图 | 九宫格 + 音视小格 | N× LoadImage + GroupReferenceToVideo | 多参考图保持主体/风格一致 |\n\n> 首尾帧一族（i2v / fl2v / fl2v_tail）的素材区**只有首帧/尾帧那几个框**，就是该模式全部的图；\n> FL2V 少填一张也能出片：只填首帧 → 自动退化成 I2V；只填尾帧 → 自动退化成 L2V（日志会回执说明）。\n\n**互不串扰（已硬保证）**：t2v 即使误传了首帧/尾帧/参考图，后端与构图层都会剔除并回传 `dropped` 列表；给 i2v 多传参考图会被忽略；i2v 不会串到 ReferenceToVideo 组。\n\n---\n\n## 9. 放大与超分（倍率统一说明）\n\n三个入口都带「**倍率**」参数（1–4，超出自动夹取）：**生图面板**、**时间线二采**、**剪辑超分**。\n\n**倍率语义（各引擎一致，不用记参数差异）**：\n\n| 引擎 | 倍率含义 | 特点 |\n|---|---|---|\n| 内置精修（hires-fix） | latent 放大倍数 | 需 Krea2 模型；二次采样，画质提升明显 |\n| SeedVR2 | **目标短边 = 源短边 × 倍率** | 质量佳但慢；图片按实际像素算，视频用 ffprobe 读 |\n| RTX VSR | 直接倍数 | 快（N 卡硬件） |\n| TE-FlashVSR | 直接倍数 | 快，需模型 |\n| **VOSR2** | 直接整数倍数 | **一步扩散超分，比 SeedVR2 快且省显存**（推荐做 H3 二采平替） |\n\n- **生图**：勾「✨ 画质增强」选引擎 + 倍率；已有图可用「✨ 再次增强」二次处理\n- **时间线**：二采行选「关闭二采 / 出片后自动 / 确定后手动」+ 引擎 + 倍率；手动模式下预览框会出现「✨ 二采高清放大」\n- **剪辑**：选素材 → 选「超分方案」+ 倍率 → 「✨ 超分放大」（会先弹文件夹让选保存位置）\n\nVOSR2 未就绪时，生图面板的选项会**自动置灰并说明原因**（模型文件或节点缺失），不会\"点了没反应\"。\n\n---\n\n## 10. 生图面板 & 分镜九宫格故事板\n\n### 10.1 四种生图模式\n\n| 标签 | 模式 | 说明 |\n|---|---|---|\n| ✨ 文生图 | t2i | 纯提示词出图 |\n| 🖼️ 图生图 | i2i | 给原图 + 强度（0.05–0.95）改写 |\n| 🌟 参考生图 | ref | 1–3 张参考图锁角色/风格/场景 |\n| ✏️ 编辑图 | edit | 原图 + 编辑 LoRA 做定向修改 |\n\n### 10.2 提示词预设（14 条）\n\n「📋 提示词预设」里包含角色三视图、五格资产板、UE5 角色表、表情表、服装分层拆解，以及 **4 套分镜九宫格故事板**。带尺寸的预设点「使用」会**自动套用出图尺寸**。\n\n### 10.3 LoRA（点一下就能用 · 对所有生图模式生效）\n\n在「🎨 生图」面板点 **「🎚 选择 LoRA」** → 弹出列表（来源 = 上面「LoRA 文件夹」里选定的目录）：\n\n- **点一项即生效**；可多选，最多同时叠 4 个；旁边「强度」输入框决定新选中项用多大（0–2，默认 1）\n- 浮层里有搜索框 + 「↻ 刷新」；「清空」取消全部；选完点「完成」关掉（也可以不关，接着点下一个）\n- 顶部会显示 `共 N 个 · 可加载 M 个 · ⚠不可加载 K 个 · 已选 x/4`\n\n**LoRA 生效范围**：文生图 / 图生图 / 参考生图 / 编辑图 **四种模式全部生效**（LoRA 挂在 UNETLoader → KSampler 之间，开启「画质增强」时二次采样同样走 LoRA）。**剧本面板的「生成设定图并入收藏」、流水线批量生图、Skill 面板的「生成对应资产」也都吃这个选择。**\n\n⚠️ **LoRA 必须放在 ComfyUI 能搜索到的目录**，否则加载会失败（表现为 `prompt_outputs_failed_validation`）：\n\n| 放哪里 | 说明 |\n|---|---|\n| `ComfyUI/models/loras/`（可带子目录） | 默认，直接可用 |\n| 其他磁盘/目录 | 需在 `extra_model_paths.yaml` 里登记该目录，然后重启 ComfyUI |\n\n面板已经帮你拦住了：不在 ComfyUI 搜索路径里的 LoRA 会**置灰并标注「不可加载」**，点它只会提示你怎么修；即使强行传进去，后端也会**忽略并把名字回报**（不会让你以为\"选了却在起作用\"）。\n\n**验证 LoRA 真的生效**：同种子、同提示词分别\"不带 LoRA / 带 LoRA\"各生成一张，两张图应当明显不同（实测平均像素差可到 60+）。\n\n### 10.4 分镜九宫格故事板（切格即 i2v 首帧）\n\n| 预设 | 整图尺寸 | 单格 | 适合 |\n|---|---|---|---|\n| ⑪ 写实电影感 3×3 | 1536×1536 | 512×512 | 剧情 / 悬疑 / 文艺 |\n| ⑫ 日式动漫 3×3 | 1536×1536 | 512×512 | 番剧 / 漫剧 |\n| ⑬ 竖屏短剧 3×3 | **1152×2048** | **384×683** | 竖屏短剧（抖音/快手） |\n| ⑭ 横屏 16:9 3×3 | 2560×1440 | 853×480 | B 站 / TVC / 横屏剧 |\n\n**两条必须知道的工程规则**：\n\n1. **严格 3×3 等分时，单格宽高比 ≡ 整图宽高比。** 要竖屏 9:16 首帧，就**直接出 9:16 整图**，切格即得，不用裁切。\n2. **i2v 首帧要求单格短边 > 300px。** 这就是竖屏档位定在 1152×2048 的原因（单格 384×683）。\n\n**用法**：🎨 生图 → 📋 提示词预设 → 选 ⑬ → 替换【角色描述】/【场景】→ 生成 → 得到一张 3×3 故事板 → 切成 9 张 → 时间线选 **i2v** 模式，把单格作为首帧逐镜出片。\n\n模板里已写死这些约束（Krea2 没有负面提示词通道，所以用正向禁令表达）：同一角色跨 9 格一致、严格 3×3 等分、细浅分隔线、每格主体完整不裁头、**全图禁止文字/字幕/箭头/水印/厚边框/网格错位**、九格按「建立环境→引入主角→触发事件→冲突升级→转折→情绪特写→行动推进→危机反转→收束钩子」推进。\n\n---\n\n## 11. 剪辑面板（剪映式）\n\n### 11.1 导入（只有一个入口）\n\n**📂 导入本地视频** —— 点它直接弹文件多选；点右边 `▾` 可选「选文件夹（递归）」或「浏览器上传」。\n（旧版的「导入文件 / 导入文件夹 / 浏览器导入」三个重复按钮已合并。）\n\n### 11.2 时间轴操作\n\n| 操作 | 怎么做 |\n|---|---|\n| 加片段 | 素材区选中素材 → 「＋ V1 主轨 / ＋ V2 副轨 / ＋ A1 音频轨」；或「⏩ 全部入轨」一键按顺序铺满 V1 |\n| 定位 | 点时间标尺任意位置 → 播放头跳过去（同时把预览 seek 到对应位置）；按住拖动可连续拖播放头 |\n| 裁剪 | **拖片段左右两端的把手**（吸附 0.1s）；拖的时候片段会**逐帧跟着变宽变窄**，旁边浮层实时显示「入点/出点 + 当前时长」；也可在下方编辑栏直接输入入点/出点秒数 |\n| 分割 | 把播放头放片段内部 → 编辑栏「✂ 分割」或按 `S` |\n| 复制 / 删除 | 编辑栏「⧉ 复制」「✕ 删除片段」，或快捷键 |\n| 排序 | 拖拽片段，或用片段上的 `◀ ▶` 微调 |\n| 变速 | 选中片段 → 编辑栏「变速」选 0.5× / 0.75× / 1.25× / 1.5× / 2× |\n| 音量 / 静音 | 选中片段 → 编辑栏音量滑杆（0–3，1=原声）+ 勾「静音」 |\n\n片段宽度**按时长等比**（30px = 1 秒）；带 `✂` 表示已裁剪，带 `1.5×` 表示已变速，带 `🔇` 表示已静音。\n\n### 11.3 快捷键\n\n| 键 | 作用 |\n|---|---|\n| `空格` | 预览播放 / 暂停（没加载视频且主轨有片段时，直接连播主轨） |\n| `← / →` | 播放头 ∓0.1 秒（`Shift` 为 ∓1 秒）—— 跟手、逐帧微调 |\n| `Alt + ← / →` | 微调**选中片段**的入点（每秒 0.1 秒） |\n| `Delete` / `Backspace` | 删除选中片段 |\n| `Ctrl + D` | 复制选中片段 |\n| `S` | 在播放头位置分割 |\n| `Ctrl + Z` | 剪辑撤销 |\n| `Ctrl + Shift + Z` / `Ctrl + Y` | 剪辑重做 |\n| `Ctrl + S` | 合成成片 |\n| `Esc` | 取消选中 |\n\n> 在输入框里打字时快捷键自动让位，不会误删片段。\n> ⚠️ `Ctrl + Z` 同时也是 ComfyUI 自己的「撤销工作流」（全局绑定），按一次会**同时**撤销剪辑和画布上的上一步操作 —— 这是 ComfyUI 的全局行为，用面板上的「↩ 撤销」按钮可只撤销剪辑。\n\n### 11.4 为什么现在拖拽\"丝滑\"\n\n- 拖拽期间**不做任何全量重绘**：每个动画帧只改被拖那一个片段的宽度 + 刷新浮层文字（用 `requestAnimationFrame` 合并高频 `pointermove`）；\n- 标尺刻度按\"总时长\"缓存，总时长没变就不重建（避免每次拖拽重建上百个 DOM 节点）；\n- 素材区点选只切换选中样式、**复用已有卡片 DOM**（不再重建整排缩略图，素材上百个也不卡）；\n- 「⏩ 全部入轨」「↻ 刷新」改为**批量一次渲染**，探时长结果带缓存（同一素材不重复 ffprobe）；\n- 预览播放 / 拖动播放头时，播放头只改 `left` 一个属性（零重排），并跟着视频播放实时走。\n\n### 11.5 合成\n\n选好轨道 → 可选背景音乐（单个文件或多段 A1 轨）→ 「🎬 合成成片」。\n后端会逐段按 `入点/出点 + 变速 + 音量` 重编码，再无损拼接、混音，输出到 `input/{资产文件夹}/video/`。\n\n---\n\n## 12. Skill 提示词优化\n\n**② 本地模型**：选 GGUF → 会自动套最优参数（上下文/采样/mmproj）→ 勾「选模型自动加载」或点「加载模型」（首载 1–5 分钟）。\n\n**③ 优化**：优化方向（含 H3 官方 T2V/I2V/FL2V/L2V/R2V 模板、角色三视图、分镜脚本）→ 填待优化内容 → 「开始优化」。勾「接续推理直到完整」可在被 token 上限截断时自动续写。\n\n**④ 推理过程**（右下角）：勾了「② 本地模型 → 思考模式」时，模型的分析过程会显示在这里，方便判断它有没有理解偏；没开思考模式会提示你开启。\n\n优化结果可直接「结果 → 剧本」回填，或「⚡ 生成对应资产」自动识别里面的角色/场景定义并出设定图入库。\n\n---\n\n## 13. 主题外观与缩放适配\n\n点顶部主题按钮循环切换：\n\n| 主题 | 效果 |\n|---|---|\n| 🎨 默认 | 冷色深蓝 + 你自选的主题色 |\n| ⚡ 蓝色流光 | 电光蓝配色 + 能量网格 + 面板头能量线流动 + 主按钮脉冲扫光 |\n| 🔥 火焰流动 | 熔金→熔岩橙→烈焰红 + 底部火舌起伏 + 飞升火星 + **点击按钮火焰向四周喷发** |\n\n- **一键改主题色**：6 个预设圆点 + 自定义取色器。改色后**所有面板的按钮、导航、输入框、卡片、面板描边、标题色阶会同步跟随**（主题自带的动效层与背景色按主题固定）\n- 切回「🎨 默认」会**还原你自己选的颜色**（不残留上一个主题的色）\n- **所有面板随节点尺寸等比缩放**：把节点拉大，导航/字号/间距/宫格/预览框全部等比变大；缩小时等比变小，不会挤爆也不会大片留白\n- 火焰主题的点击喷火只在火焰主题下生效，其他主题不受影响\n\n---\n\n## 14. 缓存与清理\n\n### 时间线「🗑 清空」会自动清三类缓存\n\n点一次「🗑 清空」= 清空分镜 + 清缓存，避免旧缓存影响后续生成：\n\n| 清什么 | 为什么 |\n|---|---|\n| 面板内状态（分镜配置 / 选择 / 出片预览 / 实时预览轮询） | 切模式或重跑时不会残留上一轮的预览帧 |\n| `output/mrnext_preview/*.jpg`（采样实时预览图） | 残留会让下一轮在写出首张预览前显示上一轮的旧帧 |\n| `input/{folder}/_plan.json` → **归档**为 `_plan.prev.json` | 残留会让节点认为还有分镜计划，从而重跑旧计划干扰新生成 |\n| `output/mrnext_thumbs/*.jpg`（素材缩略图） | 源文件变了但缓存在，会显示旧图 |\n\n日志会回执：`🧹 缓存已清理：采样预览图 N · 素材缩略图 M · 旧计划已归档`。\n\n### 其他需要手动清的情况\n\n| 位置 | 何时清 |\n|---|---|\n| `output/mrnext_preview/` | 觉得实时预览串了别的任务 |\n| `output/mrnext_thumbs/` | 缩略图与素材对不上 |\n| `user/mrboard_favorites_next.json` | 收藏库乱了（删掉会重建） |\n| 浏览器 `localStorage` 的 `mrnext.*` | 面板状态异常（也可在节点报错条上点「🧹 重置面板状态并重新加载」） |\n\n---\n\n## 15. 常见问题排查\n\n| 症状 | 原因 | 解决 |\n|---|---|---|\n| 画布搜不到节点 | 包没解压完整 / 没重启 | 检查 `custom_nodes/ComfyUI_MRBoard_Next/__init__.py` 与 `vendor/` 是否存在，重启 ComfyUI |\n| 面板全空白 | 浏览器缓存旧前端 | **Ctrl+F5 硬刷新**；仍空白就 F12 看 Console 里的 `MRBoardNext` 报错 |\n| 版本徽章不是最新 | 同上 | Ctrl+F5；确认 `main.js` 顶部 `MRNEXT_VERSION` 与包版本一致 |\n| 模型下拉是空的 | 模型放错目录 | 对照第 3 节路径表，重启一次 |\n| 面板下方一大片空白 | 旧版布局问题 | v1.9.1+ 已修（编辑区撑满），升级即可 |\n| 生图报 `got an unexpected keyword argument 'enhance_scale'` | v1.9.0–1.9.3 的签名 bug | 升级到 **v1.9.4+** |\n| 选「VOSR2」没反应 / 提示缺失 | 节点包或模型不全 | 装 `ComfyUI-VOSR2` + `models/vosr2/` 模型后重启；面板会给出具体缺什么 |\n| 文生视频画面不按提示词走 | ① CLIP 用了 4bit `nvfp4_awq` ② 旧版英文兜底稀释中文 | ① 「🧠 模型」换成 int8 版 ② 升级到 v1.7.7+ |\n| 出片时预览显示的还是上一轮的图 | 旧版预览没绑定本次任务 | 升级到 v1.9.0+（预览带 `?since=` 只认本次生成的图） |\n| 画面里出现奇怪 token（如 `@image#1:xx.png`） | 粘贴进了\"虚拟引用\" | v1.7.5+ 已在四层净化（粘贴入口 / store / 发送层 / 后端）；升级即可 |\n| 人物张嘴说胡话 / 说话乱说 | 提示词缺三段式声音字段 | v1.7.6+ 已自动组装三段式；确认「🎙️ 声音」页没关掉 |\n| 音轨失真 / 变噪音 | 步数 <8（ComfyUI 稳定版 bug） | 用 8 步以上，或升 nightly；护栏也会自动抬步数 |\n| 想跑 4–6 步加速 | 需 nightly + 蒸馏 LoRA | 升 nightly；LoRA 强度在 0.5–1.0 扫 |\n| 节点被预览大图撑高 | 旧版会给 OUTPUT_NODE 加画布预览 | v1.7.8+ 已屏蔽 |\n| 长时段连跑 OOM | 显存累积 | 时间线勾「段间清理显存（每2镜）」 |\n| 出片很慢 | 正常 | 864×480 / 12 步在 16G 卡上约 1–3 分钟/镜 |\n| 剪辑合成报未找到 ffmpeg | 缺 `imageio_ffmpeg` | 按第 2.2 节装依赖 |\n| 剪辑导入按钮点了没反应 | 系统文件选择框被拦 | 点 `▾` 用「浏览器上传」兜底 |\n\n---\n\n## 16. 产物路径速查\n\n| 类型 | 路径 |\n|---|---|\n| 出片视频 | `ComfyUI/input/{资产文件夹}/video/` |\n| 剪辑成片 | `ComfyUI/input/{资产文件夹}/video/compose_*.mp4` |\n| 二采放大视频 | 弹窗选择的目录，或源视频同目录 |\n| 生图结果 | `ComfyUI/input/{资产文件夹}/` |\n| 分镜计划 | `ComfyUI/input/{资产文件夹}/_plan.json`（清空时归档为 `_plan.prev.json`） |\n| 收藏库 | `ComfyUI/user/mrboard_favorites_next.json` |\n| 采样预览缓存 | `ComfyUI/output/mrnext_preview/*.jpg` |\n| 素材缩略图缓存 | `ComfyUI/output/mrnext_thumbs/*.jpg` |\n| 历史版本包 | `ComfyUI/custom_nodes/_packs/`（同时备份到 `Downloads`） |\n\n---\n\n## 附：目录结构\n\n```\nComfyUI_MRBoard_Next/\n├── __init__.py            # 节点注册 + vendored H3 引擎加载\n├── requirements.txt\n├── README.md              # 本文件（安装流程 + 使用教程）\n├── nodes/studio_node.py   # MRBoardStudio 节点（UI 宿主 + 出片 subgraph）\n├── server/                # /mrnext/* 后端路由\n│   ├── api.py             # 核心 API（split/analyze/h3_shot/upscale/compose/...）\n│   ├── h3shot.py          # H3 出片引擎（读 vendored 模板）\n│   ├── h3prompt.py        # H3 官方三段式提示词组装 + 低步数音频护栏\n│   ├── generation.py      # Krea2 生图 + 放大超分构图\n│   ├── skills.py          # 本地 Qwen 提示词优化（含推理过程抽取）\n│   └── favorites.py       # 收藏库\n├── web/js/                # 前端（Shadow DOM 模块化）\n│   ├── main.js            # 扩展入口（版本徽章 / 屏蔽原生预览 / 锁尺寸）\n│   ├── app.js             # 应用外壳（导航 / 主题 / store）\n│   ├── core/              # dom / store / api / mentions / styles / purify / ui\n│   └── panels/            # 10 个面板\n└── vendor/ComfyUI_MiniMaxH3_Director/   # 内嵌官方 H3 出片引擎\n```\n\n---\n\n*文档对应版本 v1.9.4。遇到问题先对照第 15 节表格；仍解决不了，把 DevTools Console 里的报错 + 面板版本号一起发出来。*\n",
  "status": "curated"
}
